
## 15:55:43
官方定价页 api-docs.deepseek.com/quick_start/pricing 显示当前 DeepSeek API 提供两个模型：deepseek-flash（版本 DeepSeek-V4.1-Flash）与 deepseek-v4-pro（版本 DeepSeek-V4-Pro-0813）。均支持 thinking/non-thinking 模式（默认 thinking）。上下文长度 1M，最大输出 384K。功能：JSON 输出、工具调用、Responses API、Anthropic API、Chat Prefix Completion(Beta)、FIM Completion(Beta，仅非思考模式)；Vision 仅 flash 支持，Pro 不支持。并发限制 flash 2500、pro 500。来源：api-docs.deepseek.com

## 15:55:43
定价（每 1M tokens，美元，api-docs.deepseek.com 定价页）：
deepseek-flash：输入缓存命中 off-peak $0.003 / peak $0.006；输入缓存未命中 off-peak $0.15 / peak $0.30；输出 off-peak $0.60 / peak $1.20。
deepseek-v4-pro：输入缓存命中 off-peak $0.022 / peak $0.044；输入缓存未命中 off-peak $0.66 / peak $1.32；输出 off-peak $1.98 / peak $3.96。
off-peak 为 peak 的一半。Peak 时段：UTC 周一至周五 01:00-04:00 与 06:00-10:00，其余为 off-peak。来源：api-docs.deepseek.com

## 15:57:54
模型列表（官方）：DeepSeek API 目前仅两个模型标识符 —— deepseek-flash 与 deepseek-v4-pro。来源：api-docs.deepseek.com/api/list-models（GET /models 示例返回 object:"list"，data[].id 为上述两者，owned_by:"deepseek"）。

## 15:58:07
定价（官方 quick_start/pricing，单位 1M tokens，USD）：deepseek-flash：缓存命中 off-peak $0.003 / peak $0.006；缓存未命中 off-peak $0.15 / peak $0.30；输出 off-peak $0.60 / peak $1.20。deepseek-v4-pro：缓存命中 $0.022 / $0.044；未命中 $0.66 / $1.32；输出 $1.98 / $3.96。峰谷规则：off-peak = peak 的一半；peak = UTC 01:00-04:00 与 06:00-10:00（周一至周五），其余时段 off-peak。来源 api-docs.deepseek.com。

## 15:58:07
模型规格（官方）：deepseek-flash = DeepSeek-V4.1-Flash；deepseek-v4-pro = DeepSeek-V4-Pro-0813。context length 1M，最大输出 384K；默认 thinking 模式（可切 non-thinking）。功能：JSON 输出、工具调用、Responses API、Anthropic API、前缀续写(Beta)、FIM(仅 non-thinking)；Vision 仅 flash 支持(_，v4-pro 不支持)。并发限制：flash 2500，v4-pro 500。来源 api-docs.deepseek.com/quick_start/pricing。

## 15:59:26
【定价页全文·2026-09-10版】(api-docs.deepseek.com/quick_start/pricing)：每1M token，USD，峰谷双价。
- deepseek-flash（=DeepSeek-V4.1-Flash）：缓存命中 off-peak $0.003 / peak $0.006；缓存未命中 $0.15 / $0.30；输出 $0.60 / $1.20。
- deepseek-v4-pro（=DeepSeek-V4-Pro-0813）：缓存命中 $0.022 / $0.044；未命中 $0.66 / $1.32；输出 $1.98 / $3.96。
- 规格：上下文 1M，最大输出 384K，默认 thinking（可切 non-thinking）。功能：JSON Output、Tool Calls、Responses API、Anthropic API、Chat Prefix Completion(Beta)、FIM（仅非thinking）；Vision：flash ✓，v4-pro ✗。
- 并发上限（修正旧的25/截断值）：deepseek-flash 2500，deepseek-v4-pro 500。
- 脚注(3)：闲时价=峰时价一半；高峰时段 01:00-04:00 与 06:00-10:00 UTC，周一至周五，其余全为闲时。→ 折算北京时间 09:00-12:00、14:00-18:00，与第三方报道一致，矛盾消解。
- 脚注(1)：deepseek-v4-flash 与 deepseek-v4-flash-vision-exp 为遗留名，模型已退役，请求由 V4.1-Flash 承接并按 Flash 价格计费。
- 扣费规则：费用=token数×单价，从充值余额或赠送余额扣，两者都有时优先扣赠送余额。
- 官方免责声明：价格可能变动，DeepSeek 保留调整权，建议按实际用量充值并定期查看本页。
来源域名：api-docs.deepseek.com

## 15:59:40
【限流页全文】(api-docs.deepseek.com/quick_start/rate_limit)：
- 并发上限（按账号计，与用哪个 API Key 无关）：deepseek-flash 2500，deepseek-v4-pro 500。请求自发出至响应完成计为1个并发；超限返回 HTTP 429。
- 可提交扩容申请（capacity expansion request），按实际业务匹配并发，**扩容不额外收费**。
- user_id 隔离：可用于内容安全隔离、KVCache 隐私隔离、调度隔离；普通用户所有 user_id 合并计算并发；已扩容用户则对每个 user_id 单独限流（flash 2500 / v4-pro 500），空 id 视作特殊 user_id。user_id 须匹配 [a-zA-Z0-9\-_]+，最长 512 字符，禁止含隐私信息。
- Keep-Alive：非流式请求持续返回空行，流式请求持续返回 SSE `: keep-alive` 注释；若请求 10 分钟内未开始推理，服务器关闭连接。
来源域名：api-docs.deepseek.com

## 15:59:54
【/updates 变更日志补充】(api-docs.deepseek.com/updates)：
- 2026-09-10：发布 DeepSeek-V4.1-Flash（新架构家族最小模型，原生多模态视觉理解）。★MathArena Apex: 65.6（此前截断，已补齐）；另有 Terminal-Bench 2.1 90.6、3.0 30.0、4.0 31.2、DeepSWE v1.1 74.2、CyberGym 88.1、SEC-Bench Pro 62.8、HLE w/tools 63.9 等。调用名改 deepseek-flash；前代 V4 Flash、V4 Flash Vision Exp 已退役，旧名 deepseek-v4-flash / deepseek-v4-flash-vision-exp 暂时路由到 V4.1 Flash。
- ★2026-09-10 同页：DeepSeek 决定 2026-09-14 之后继续提供 DeepSeek V4 Pro 的 API 服务，计费方式不变（此前疑似有下线计划）；如有变化将另行通知。
- ★2026-09-10：随 V4.1-Flash 发布，API 价格相应下调（"API prices have been reduced accordingly"，详见 Models & Pricing）。→ 说明现行价格自 2026-09-10 起生效。
- 2026-08-21：发布多模态视觉模型 DeepSeek-V4-Flash-Vision-Exp（实验性，model='deepseek-v4-flash-vision-exp'）。
- 2026-08-13：DeepSeek-V4-Pro GA 上线 APP/Web/API；API 原生支持 OpenAI Responses API 并针对 Codex 适配；thinking 支持 low/high/max 三档努力级别（V4-Pro 与 V4-Flash）。
来源域名：api-docs.deepseek.com
