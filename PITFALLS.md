# Agent 踩坑清单 —— 各节点易错问题与解决方案

> 用途：记录 20+ 节点中容易忽略、后果严重的问题，供日常开发对照检查。
> 每条格式：**容易忽略的点 → 造成的状况 → 最终效果 → 如何解决**。
> 全部条目都有本项目真实踩坑记录背书（详见 notes/ 各阶段复盘）。

---

## 一、模型调用与消息层

### 1. messages 必须回填 assistant 的 tool_calls 消息
- **容易忽略**：以为只要把工具结果给模型就行，漏掉把模型上一轮的 assistant 消息（含 tool_calls 声明） append 进历史。
- **造成的状况**：下一轮请求要么直接 400 报错，要么模型"忘记"自己点过菜，行为错乱。
- **最终效果**：轻则一次调用失败，重则 agent 在同一任务上无限绕圈。
- **解决**：工具结果回填前，先把 `messages.append(msg)`（含 tool_calls 的 assistant 消息）加上；用 trace 校验消息顺序：`assistant(tool_calls) → tool → tool → 下一轮请求`。

### 2. tool_call_id 必须严格一一配对
- **容易忽略**：模型一次点多个工具时，回填漏一条、或 id 抄错。
- **造成的状况**：API 直接 400（每个 tool_call 必须有对应 tool 消息）；或模型把 A 工具的结果当成 B 工具的。
- **最终效果**：循环崩溃或决策建立在错乱事实上。
- **解决**：回填用循环逐条对应（`for tc in msg.tool_calls`）；写个校验函数：请求里 call_ids 集合 == tool 消息 id 集合。

### 3. tool_calls 的 arguments 是 JSON 字符串，不是对象
- **容易忽略**：拿到 `tc.function.arguments` 直接当 dict 用。
- **造成的状况**：AttributeError 或参数解析失败。
- **最终效果**：工具执行崩溃，错误还会污染上下文。
- **解决**：始终 `json.loads(tc.function.arguments)` 后再传参。

### 4. 第三方 SDK 的 pydantic 对象用蛇形命名（本项目踩了 4 次！）
- **容易忽略**：按协议 JSON 里的驼峰字段名（`inputSchema`、`serverInfo`）访问 SDK 对象。
- **造成的状况**：`AttributeError: object has no attribute 'inputSchema'`；且在多智能体场景会被 anyio TaskGroup 包成 ExceptionGroup，真异常被埋住。
- **最终效果**：排查时间成倍放大（MCP 接入时此问题拖了三轮调试）。
- **解决**：用第三方 SDK 的 pydantic 对象前先 `dir()` 或 `model_dump()` 看真名；异常处理永远打印完整 traceback。

### 5. finish_reason=length ≠ 任务完成
- **容易忽略**：循环停止条件只判"没有 tool_calls 就算完成"。
- **造成的状况**：模型输出被 max_tokens 截断，半截话被当成最终答案。
- **最终效果**：交付物残缺且毫无提示，用户以为拿到了完整结论。
- **解决**：停止判断用 `finish_reason == "stop"`；length 时提示模型继续或扩展预算。

### 6. 角色与协议的厂商差异
- **容易忽略**：把 OpenAI 的新角色（如 `developer`）或新参数用到兼容端点上。
- **造成的状况**：DeepSeek 直接 400，错误原文里列出合法角色（system/user/assistant/tool）。
- **最终效果**：换模型供应商时代码静默失效。
- **解决**：换供应商先跑通最小请求；把"协议差异"记入供应商适配笔记。

---

## 二、工具设计与调用

### 7. 注册表里有 ≠ 模型知道（最高频踩坑）
- **容易忽略**：工具函数写好了、REGISTRY 注册了，但忘了加进发给模型的 `tools` 参数。
- **造成的状况**：Stage 4 实测：note_write 没进 tools 清单，模型全程不知道笔记存在。
- **最终效果**：agent 的发现随压缩丢失，**反复重搜同一个问题**（16-20 步里连搜 4 次"扣子价格"），成本翻倍质量下降。
- **解决**：能力唯一来源是 tools 参数；每次新增工具检查三处一致：函数实现 → REGISTRY → TOOLS 清单。

### 8. 工具 description 的措辞就是行为
- **容易忽略**：把 description 当普通注释随手写。
- **造成的状况**：描述含糊时模型乱用（参数填错、该调不调、重复调用）；删掉一句"一次只查一个产品"，模型就开始把两个产品塞进一个参数。
- **最终效果**：Anthropic 实测靠精修工具描述刷到 SWE-bench SOTA；描述质量直接等于工具质量。
- **解决**：description 按"给新同事的入职文档"写：何时用、何时不用、参数含义、边界；改动描述 = 改动行为，必须跑评测回归。

### 9. 工具错误信息要"可行动"
- **容易忽略**：工具报错时返回 `Error: timeout` 这类死信息。
- **造成的状况**：模型不知道下一步该怎么办，只能原样重试或放弃。
- **最终效果**：自我纠错机制失效，一个可恢复的错误导致整任务失败。
- **解决**：错误消息三段式——发生了什么 + 可能原因 + 下一步建议（"请换更简短的关键词重试，或改用 fetch_url 直接访问官方网址"）。

### 10. 工具参数用 enum + 最少 required
- **容易忽略**：参数用自由字符串、必填项一大堆。
- **造成的状况**：模型自由发挥填出脏值（"Notion、飞书"塞进单产品字段）；必填越多填错概率越大。
- **最终效果**：下游执行报错或拿到错误数据。
- **解决**：枚举锁死可选值；required 只留真正必需的；参数描述里写默认值和示例。

### 11. 工具结果必须在进入上下文前压缩
- **容易忽略**：工具返回什么就原样 append 进 messages。
- **造成的状况**：一个 4 万字网页原样进上下文，之后 agent 循环**每一轮**都为它付全价 token，且稀释注意力导致早期约束被"淹没"。
- **最终效果**：成本滚雪球 + context rot（模型开始违背最初指令）。
- **解决**：进门四步——正文抽取（去广告导航）→ 字段精选（title/url/snippet）→ 硬截断（单字段300、整体4000）→ 附说明（"已截断，需要更多请告诉我"）。

### 12. 并行会放大工具的速率限制
- **容易忽略**：单 agent 顺序跑搜索没事，多智能体并行后同一接口被打爆。
- **造成的状况**：Stage 7 首跑，3 个并行 worker 触发 DuckDuckGo 超时/限流，两个 worker 全程查不到数据。
- **最终效果**：多智能体的"能力提升"被工具层完全吃掉，输出大面积"未查到"。
- **解决**：重试+退避（2s/4s）；worker 错峰启动（stagger）；生产换付费搜索源；工具层增加限流保护。

---

## 三、循环控制

### 13. agent 不会自己收敛（Stage 5/7 两次验证）
- **容易忽略**：以为模型"查得差不多就会写报告"。
- **造成的状况**：实测连续两次 16 步全部在调用工具调研，从不主动收尾；多智能体首跑 3 个 worker 集体步数耗尽不交卷。
- **最终效果**：步数耗尽强制截停，任务无产出，token 白烧。
- **解决**：**收敛是 runtime 的职责**——剩 2 步时注入死线消息（"⚠️时间将尽：note_read 后立即输出报告，不要再搜索"）；主循环和子 agent 循环都要装。

### 14. 强制停止必须"优雅收尾"
- **容易忽略**：max_steps 用完直接退出/报错。
- **造成的状况**：用户只看到失败，之前查到的有价值信息全部丢失。
- **最终效果**：信任崩塌——用户不敢把重要任务交给 agent。
- **解决**：收尾三要素：汇报已有进展（带来源）+ 列出未完成项 + 说明如何续跑。预算护栏用同一套逻辑（80% 预警注入结题指令）。

### 15. 循环是严格串行同步的，没有异步
- **容易忽略**：把工具执行想成"后台任务"，以为可以"先回填再补执行"或"异步写笔记"。
- **造成的状况**：实现成异步会出现竞态（读到没写完的状态）；点了几道菜漏执行一道，下一轮请求直接 400。
- **最终效果**：状态错乱且难复现。
- **解决**：牢记节拍——每轮声明了几个工具调用、就同步逐个执行几个、全部回填，才能发下一轮。**每个调用必有对应结果，一圈一结清。**

### 16. 步数上限（max_steps）是安全阀也是质量约束
- **容易忽略**：上限设得太大（失控烧钱）或太小（正常任务做不完，Stage 3 时 12 步不够用）。
- **造成的状况**：前者死循环烧钱，后者全任务是"未完成"。
- **最终效果**：要么成本事故要么可用性事故。
- **解决**：按任务历史收敛步数 ×2 设置；stop_reason 里 max_steps 占比异常升高 = 有问题的信号，去读 trace。

---

## 四、上下文工程

### 17. 历史压缩的切分点不能落在 tool 消息上
- **容易忽略**：按"保留最近 N 条"机械切分，切在 assistant(tool_calls) 和它的 tool 结果之间。
- **造成的状况**：tool 消息的"上级调用者"被切掉，成了孤儿消息，API 直接 400。
- **最终效果**：压缩动作本身把 agent 搞崩。
- **解决**：`_safe_split`：切分点向后滑动直到不是 tool 角色为止。

### 18. 压缩后不要人造 assistant 消息
- **容易忽略**：压缩重建上下文时，习惯性补一条"好的，我继续"的 assistant 确认消息。
- **造成的状况**：DeepSeek 思考模式要求每条 assistant 消息带 `reasoning_content`，人造消息没有它 → API 400："must be passed back"。
- **最终效果**：压缩功能整体不可用，且报错信息不直观。
- **解决**：摘要以 user 消息注入，直接衔接原文历史；凡是"构造消息"都要核对协议对 role 的完整性要求。

### 19. 压缩是有损的，必须配套笔记
- **容易忽略**：以为上了 compaction 就万事大吉。
- **造成的状况**：实测无笔记时，压缩把查到的定价数字压没了，模型反复重搜；纯文字反思修订也因为"证据被压没"而失败。
- **最终效果**：成本不降反升，质量下降。
- **解决**：**压缩+笔记是配套能力**：压缩负责上下文短，笔记负责事实不丢；关键发现随手 note_write，写报告前 note_read。

### 20. 压缩摘要要指定"必须保留的信息类别"
- **容易忽略**：让模型"把历史总结一下"，不列保留清单。
- **造成的状况**：摘要写了流水账，丢了任务目标、约束和数据。
- **最终效果**：压缩后模型偏题、违反早期要求、数据缺失。
- **解决**：摘要提示词强制四类信息：任务目标 / 关键发现（含来源）/ 未完成事项 / 约束条件；总长限制。

### 21. system prompt 保持稳定的前缀位置（缓存经济）
- **容易忽略**：把经常变化的内容拼进 system prompt 开头。
- **造成的状况**：前缀变化 → 供应商的上下文缓存全部失效 → input token 全价计费。
- **最终效果**：同样的任务，成本差数倍。
- **解决**：稳定内容（规则/记忆）放最前且不动；动态内容往后放；每轮看 usage 里 cached_tokens 占比。

### 22. system prompt 是行为规则，tools 是能力声明——别写错位置
- **容易忽略**：在 system 里"宣布"工具（能力），或指望 tools 描述里写流程规则。
- **造成的状况**：system 提了没放进 tools 参数的工具 → 模型根本调不了；工具使用时机只写在 system → 工具被复用到别处时规则丢失。
- **最终效果**：能力与规则错位，行为不可预测。
- **解决**：工具的"何时用/怎么用"写进工具 description；system 只写跨工具的编排规则和全局准则；尽量工具无关表述（防改名漂移）。

---

## 五、记忆

### 23. 记忆与笔记必须划清边界
- **容易忽略**：把调研内容（某个产品的价格）当"记忆"存起来。
- **造成的状况**：一次任务的数据污染所有后续会话的 system prompt。
- **最终效果**：agent 带着过时事实"偏见"工作，越用越歪。
- **解决**：提取提示词硬性划界——只收用户本人的稳定偏好（领域/格式/语言/称呼），调研内容归笔记，临时要求不收。

### 24. 不可观测的环节坏了都不知道
- **容易忽略**：记忆提取函数静默返回空列表，没有任何打印。
- **造成的状况**：Stage 8 首跑记忆写入为空，无法判断是提取失败还是没内容可提。
- **最终效果**：功能形同虚设且无法诊断。
- **解决**：每个环节都要有回显（"提取到 N 条候选: [...]"）；关键动作可观测是底线。

### 25. 记忆需要容量上限与去重
- **容易忽略**：只增不删，以为越多越好。
- **造成的状况**：记忆膨胀成噪音，重复条目互相稀释，过时偏好长期误导。
- **最终效果**：注入的记忆反而降低表现。
- **解决**：合并去重（相同/包含则跳过）+ 容量上限（20 条丢最旧）+ 生产上提供用户查看/编辑/遗忘接口。

---

## 六、多智能体

### 26. 并行线程共享全局状态会互相污染
- **容易忽略**：多个子 agent 线程共用一个全局 NOTES_FILE 变量，运行中互相覆盖。
- **造成的状况**：worker A 刚设置的笔记路径被 worker B 覆盖，笔记写串。
- **最终效果**：数据错乱且难复现（时序依赖）。
- **解决**：thread-local 存线程专属资源（`threading.local`）；或干脆每个任务用独立进程。**通用原则：需要留存的历史状态，写入时立刻快照（deepcopy），不存引用**——Stage 9 之前"三次捕获全是最后一次"就是这个病。

### 27. 子 agent 的任务书必须自包含且互不重叠
- **容易忽略**：任务书写成"查一下 Coze"这种半句，指望子 agent 看到用户原话。
- **造成的状况**：子 agent 看不到任务书之外的上下文，范围理解错；两个 worker 调研范围重叠，重复劳动。
- **最终效果**：并行优势变并行浪费。
- **解决**：任务书三要素：调研目标 + 要查证的具体信息 + 边界（"不要碰其他子任务范围"）；orchestrator 提示词里硬性要求"任务自包含、不得重叠"。

### 28. 拆分要 effort scaling：能不拆就不拆
- **容易忽略**：简单问题也硬拆成多个子 agent，显得"架构先进"。
- **造成的状况**：每个子 agent 重建上下文的成本 > 并行收益；简单问题拆分纯属烧钱。
- **最终效果**：成本翻倍，速度反而更慢。
- **解决**：拆分提示词写明"问题简单到一次调研能完成就只给 1 个"；Anthropic 的量级参考：简单查询 1 个 agent 几次调用即可。

### 29. 诚实验收要写进每一级 prompt
- **容易忽略**：只给最终报告环节写"不许编造"，子 agent 没写。
- **造成的状况**：子 agent 编数据 → 主 agent 拿着假数据合成一份"看起来完美"的假报告。
- **最终效果**：错误以高置信度交付，比明显失败危害大得多。
- **解决**：每级都写"未查到如实标注'未查到'"；实测子 agent 全军覆没时，主 agent 输出"本次调研失败，请勿用于决策"——护栏沿责任链传导。

---

## 七、安全件（HITL / 护栏 / 记忆安全）

### 30. HITL 只拦真正危险的动作
- **容易忽略**：为了"安全"，所有工具调用都要人工批准。
- **造成的状况**：研究 agent 99% 的动作是只读搜索，全拦 = 批准疲劳。
- **最终效果**：用户无脑按 y，确认机制形同虚设，还多了无数打断。
- **解决**：按危险分级——不可逆动作 / 对外发送 / 花钱 / 改权限 / 低置信决策才拦截；只读工具静默放行。

### 31. 拒绝的回执话术决定 agent 的后续行为
- **容易忽略**：人工拒绝后只回填"用户拒绝了"。
- **造成的状况**：模型不死心，换个说法再试一次（用户已经否了还纠缠）。
- **最终效果**：体验极差，用户感觉失控。
- **解决**：拒绝回执写全："用户已拒绝，**不要重复尝试**同一动作，改为直接在回答中输出内容并告知用户可自行转发。"实测模型收到后完全服从。

### 32. 外部 MCP 工具默认不可信
- **容易忽略**：接入 MCP 生态时，把 server 给的工具当自家工具同等信任。
- **造成的状况**：外部 server 的 write/edit 类工具绕过了危险分级，直接改本地文件。
- **最终效果**：安全体系被"生态接入"打穿。
- **解决**：外部工具接入时自动扫描名字（write/edit/move/create）升级为危险级走 HITL；生产上再加白名单。

### 33. 预算收尾本身也耗 token
- **容易忽略**：把预算触发线设成 100%，以为"到线即停"。
- **造成的状况**：实测 3000 预算最终用了 4563——收尾那次调用（读笔记+写结题报告）本身就超了线。
- **最终效果**：预算形同虚设，总在小额超支。
- **解决**：触发线设在预算的 80%（`near_limit(0.8)`），给收尾调用留余量；预算判断用真实计费值（usage）而非估算。

---

## 八、工程实践（跨节点通用）

### 34. 统一入口才能统一观测
- **容易忽略**：新代码图省事直接调 `client.chat.completions.create`，绕过统一封装。
- **造成的状况**：Stage 6 三方实验里，A/B 两方案的 token 计量显示为 0——录制器挂在统一入口上，绕过即失明。
- **最终效果**：对比数据失真，错误结论（差点得出"workflow 零成本"的荒谬结论）。
- **解决**：所有 LLM 调用走同一扇门（`call_llm` 支持 `**extra` 透传参数）；观测钩子（RECORDER）挂在门上。

### 35. 调试优先级：先查工具与观测代码，再怀疑模型
- **容易忽略**：模型行为异常时第一反应是"prompt 写得不好"或"模型不行"。
- **造成的状况**：本项目多次"模型表现诡异"，真凶分别是：日志脚本存引用没拷贝（三次记录全是最后一次）、工具没进 tools 参数、压缩模块类型假设错误。
- **最终效果**：改半天 prompt 无效，浪费时间还污染了原本正确的 prompt。
- **解决**：排查顺序固定为——① trace 数据可信吗（快照/配对/计量）？② 工具执行和回填正确吗？③ prompt 与工具描述最近改过什么？最后才轮到"换个模型试试"。

### 36. 结论必须建立在多题均值上
- **容易忽略**：跑一次实验看到分数变化就下结论。
- **造成的状况**：实测同一道题两次运行分数差 0.2+；反思增强在一次实验里"反超"，换批题就垫底。
- **最终效果**：基于噪声做决策，方向性错误。
- **解决**：改动前后各跑完整评测集（≥12 题）；差异小于噪声带宽的变化不采信。

### 37. 长文档的结构性内容会被多次编辑弄乱
- **容易忽略**：往 Markdown 手册里反复"替换插入"章节。
- **造成的状况**：AGENT_NODES.md 的节点预告表在三次编辑后出现孤儿表头、表行漂移到错误章节。
- **最终效果**：学习材料自相矛盾，误导后续复习。
- **解决**：结构性文档改完必须通读校验标题层级；重要表格变更用脚本或锚点维护。

---

### 44. 软性收尾指令挡不住预算超支——到线要"拔工具箱"
- **容易忽略**：80% 时注入"请停止调研输出结题报告"的 user 消息，以为模型会照办。
- **造成的状况**：实测三次运行全部越线（61581/60000、4563/3000、60588/60000）——指令是"请求"，模型仍继续发起工具调用，之后强制结题又多烧一笔。
- **最终效果**：预算形同虚设，每次都小幅超支。
- **解决**：两级机制——80% 软提醒 + **95% 硬收尾（最后一次调用不传 tools 参数，模型物理上无法再发起工具调用，只能输出结题报告）**。物理约束 > 行为请求。

### 45. MCP 客户端/子进程的生命周期泄漏
- **容易忽略**：MCPClient 后台线程 `while True` 保活、没有任何 close 路径；每次运行新建 client = 每次 spawn 一个 npx 子进程常驻。
- **造成的状况**：跑 10 次带 MCP 的任务 = 10 个僵尸 server 进程 + 10 条死线程。
- **最终效果**：内存/进程表缓慢膨胀，长期运行的主机被拖垮。
- **解决**：① close() 三步——stop 事件信号 → stdio 上下文退出 → join 线程；② **共享单例**（get_shared_client：同 server 全项目只连一次）；③ 兜底强杀（join 超时后按参数特征 pkill 自己启动的子进程）；④ atexit 兜底。注意 pkill 模式只匹配自己的启动参数，避免误伤。

## 附：最容易被忽视的 TOP 5（如果只记五条）

1. **注册表里有 ≠ 模型知道**——模型只认 tools 参数（#7）
2. **agent 不会自己收敛**——死线注入是 runtime 的职责（#13）
3. **每轮全量重发历史**——所以进门压缩最便宜、旧结果必须清理（#11）
4. **工具描述的措辞就是行为**——改一句话就是改行为（#8）
5. **先查工具与观测，最后才怀疑模型**（#35）

---

## 九、预算与容错（Stage 11 新增）

### 38. 预算耗尽的硬停路径会产生"空报告"
- **容易忽略**：预算耗尽时直接 `break` 退出循环，没有兜底产出。
- **造成的状况**：模型在收尾指令注入后仍继续发起工具调用 → 100% 硬停 → `final` 还是空字符串 → 用户拿到**一份空报告**（GUI 上就是"没有最终结果"）。
- **最终效果**：整个任务的发现全部丢失，用户以为 agent 坏了。
- **解决**：非正常停止（预算/步数/中止/错误）且无产出时，**强制做一次无工具结题调用**（追加"预算已用尽，输出阶段性结题报告"消息，不带 tools 参数杜绝再次发起调用）。任何停止原因都必须有交付物。

### 39. 证据获取失败时模型会无限重试，需要"失败熔断器"
- **容易忽略**：目标信息在网站上不存在（或搜索引擎超时）时，模型不会主动放弃——实测为找一个不存在的定价，35 次工具调用烧穿全部预算。
- **造成的状况**：搜索失败还叠加重试退避（每次失败 7 秒+），一个任务拖 8 分钟且颗粒无收。
- **最终效果**：预算被"找不到"吃光，正常任务被饿死。
- **解决**：失败熔断器——连续 4 次工具结果以"错误"开头 → 注入强制改道指令（"禁止再用相同方式尝试，基于已有信息输出阶段性结论"）；最多触发 2 次；有成功结果即清零计数。

### 40. 注入消息绝不能插在 assistant(tool_calls) 与 tool 结果之间
- **容易忽略**：熔断器/任何"中途注入的消息"，为了"及时"直接 append 进当前轮次的消息序列。
- **造成的状况**：user 消息插在 assistant(tool_calls) 和 tool 结果中间 → 破坏"tool 消息必须紧跟 tool_calls"的配对规则 → API 400 → 异常裸奔出循环 → 整个 run 崩溃且无任何输出。
- **最终效果**：比不加熔断器更糟（原本只是慢，现在直接崩）。
- **解决**：**注入时机必须在全部 tool 结果回填之后**（内层循环只记标记，外层统一注入）；模型调用包 try/except，协议报错降级为"错误停止+强制结题"而不是炸出进程。
- **变体（实测）**：硬收尾路径也有同款雷——预算到线时模型**刚声明了调用还没执行**，此时做无工具结题调用，历史里躺着"未应答的 tool_calls"→ 结题调用 400 → 兜底文案顶替了真正的报告。解决：强制结题前先 `_trim_unanswered_tool_calls()`（摘除末尾未应答的 tool_calls 声明，语义=那轮作废）。**凡是"带着当前历史发新请求"的地方，都要先确认协议状态合法。**
- **⚠️ 元教训（最重要的部分）**：这个 bug 的修复**第一次悄悄失败了**——`str.replace()` 在锚点不匹配时静默 no-op，不报任何错；而"验证通过"的那次运行恰好没走到硬停路径，于是假修复带着假验证上了线，用户再次踩坑才暴露。两条纪律：① **凡 replace 必断言**（`assert old in s`，replace 后验证计数）；② **验证必须覆盖目标路径本身**（要测硬停，就制造硬停，LOW_BUDGET 就是干这个的），"类似任务跑通了"不等于"这个分支修好了"。

### 41. JS 渲染页面：静态抓取拿到的是"应用壳"
- **容易忽略**：网站是 JS 应用时，服务器返回的 HTML 只是壳子（实测：yourtools.xyz 原始 HTML 2 万字符，可见文本仅 1079 字，用户在浏览器看到的"页脚定价"完全不在其中）。
- **造成的状况**：fetch_url 抓到的是营销文案壳，定价数据 0 命中；模型反复尝试后只能如实报告"未查到"。
- **最终效果**：不是工具坏了，是**该内容在 JS 执行前根本不存在**。
- **解决**：三层诊断法定位（① 查原始 HTML 有无目标关键词 → ② 查可见文本占比判断是否 SPA → ③ 查 JS 文件/懒加载），然后加 `fetch_js` 工具（Playwright 无头浏览器：渲染 + **滚动触发懒加载** + inner_text 提取页脚）。实测滚动前后差 13 行价格数据。**另有 UA 大坑：怪异自定义 UA 会让网站 JS 走不同渲染分支，定价区整个不出现——用默认浏览器 UA。**

### 42. append(list) 会把整个列表嵌套成一个元素
- **容易忽略**：新工具声明写成 `REAL_TOOLS.append(FETCH_JS_TOOL)`，而 FETCH_JS_TOOL 本身是列表。
- **造成的状况**：`REAL_TOOLS[2]` 变成嵌套列表 `[{...}]`，API 400：`unknown variant 'type'`——报错信息完全看不出是嵌套问题。
- **最终效果**：新增的工具永远无法使用，且第一步调用就失败。
- **解决**：列表合并用 `extend`；加结构断言 `assert all(isinstance(t, dict) for t in REAL_TOOLS)`。

### 48. 记忆合并的子串判重挡不住"近重复"
- **容易忽略**：去重用"新文本是否为旧文本的子串"判断，措辞微调/多一个空格就穿透（实测："结论用表格对比呈现" vs "结论**偏好**用表格形式对比呈现"、"国内AI产品" vs "国内 AI 产品"各自重复入库）。
- **造成的状况**：记忆条目持续膨胀，同一偏好注入 system 多次——浪费 token，且两条措辞相近的偏好可能互相冲突。
- **最终效果**：system 里带着重复记忆跑，用户以为 agent"记性好"，实际是垃圾没清。
- **解决**：merge 升级为 **LLM 语义合并**（同义合并/冲突以新为准/格式统一，JSON mode 输出），规则判重降级兜底；**合并时保留原时间戳**（旧条目不因改写而"变新"）。实测：近重复合并条数不涨 ✓、真新偏好正常收录 ✓。

### 48-b. 证据截断的"头部偏置"会切掉页脚定价（supported 低的架构根因之一）
- **容易忽略**：进门截断只保留头部 N 字符时，页面中后部的高价值信息（定价表、页脚联系方式、FAQ）整段丢失——模型"看不到"就会用参数记忆补全，表现为**编造数字**。
- **造成的状况**：基线评测 supported=0.65，q01 的 judge 评语直接点出"定价表大部分具体数字在证据中未出现"。
- **最终效果**：有据性分数被证据覆盖率压住，提示词调优无法根治。
- **解决**：`smart_truncate` 头尾保留截断（前 70% + 后 30% + 中间标记）；web_search 的 JSON 截断改为"丢末尾条目保结构"。实测 DeepSeek 定价页 64 个价格片段在截断后存活，q01 重跑 0.79。更深的根治：Tavily raw_content 或识别定价类页面动态加大配额。

### 50. max_steps 出口只丢占位符，不做强制收尾（不限档首跑即触发）
- **容易忽略**：预算路径装了"拔工具箱+笔记兜底"的强制收尾，但 **max_steps 路径只塞一句占位符**"（达到最大步数未能完成任务…）"——两个出口不同权。
- **造成的状况**：不限额度档 + 深度任务（实测 20 步、119,842 tok）步数耗尽 → 用户拿到一句占位符，而模型在最后两轮已把全部调研成果写进笔记——**信息在，交付物没了**。
- **最终效果**：不限档反而交出最差的报告；且 RunLog 曾把此类停止误记为 model_done（现已修正为 max_steps 并强制收尾）。
- **解决**：max_steps 出口复用硬收尾三连——`_trim_unanswered_tool_calls`（末尾未应答点菜作废）→ 追加结题指令 → **无 tools 强制结题调用** → 事件发射（含输出预览/失败信息）。死线注入同时从 `==` 改为 `>=`（最后两步都提醒）。

### 51. 裸 emit 的事件缺 sub 字段，前端层级渲染直接崩
- **容易忽略**：内核里 `emit("budget", ...)` 和 `_emit_sub("budget", ...)` 混用——前者不带 `sub` 编号，后者的编号是前端"第 N 步"分组折叠的分组键。
- **造成的状况**：预算硬收尾/步数耗尽路径发出的事件到前端走 `addSub(sub.split('.'))`，对 `undefined` 调 `.split` 抛 TypeError——该事件卡片静默丢失，用户看不到"强制结题"这条关键解释。
- **最终效果**：恰恰是最需要向用户解释的异常出口（预算硬线、步数耗尽）在界面上消失。
- **解决**：三处统一改 `_emit_sub`；前端 `addSub` 加防御（sub 为空时降级为独立卡片，兼容旧存档回放）。**校验方法**：跑一次 LOW_BUDGET 触发硬收尾，遍历 SSE 流断言所有层级事件都带 sub。

### 52. 运行注册写了两遍：app.py 一次 + agent_core 一次（后者还引用未定义变量）
- **容易忽略**：会话管理接线时在 `start_research` 里 `session.add_run(run_id)`，但内核 run() 结尾还残留一行 `self.session.add_run(self.run_id or rec["run_id"], ...)`——而 `rec` 要到下一行 `log_run()` 才定义。
- **造成的状况**：web 路径靠 `or` 短路侥幸不炸（run_id 恒为真值），但会话 meta 里每个 run 记**两条**；CLI 路径若 run_id 为空则结尾 NameError，整个 run 白跑。
- **最终效果**：会话运行历史翻倍，统计口径错乱。
- **解决**：注册只留入口一处（webapp 启动时注册——agent 半路崩溃也留痕），删掉内核里的重复行。**教训**：同一状态变更只能有一个属主，接线新入口后要全局搜旧写入点。

### 53. Web 注入层重复发事件：_web_approver 多发一条缺字段的 approval_result
- **容易忽略**：早期 approver 实现里 `self.emit("approval_result", tool=..., approved=...)`，后来内核循环升级为在 approver 返回后统一发带 `sub` + `receipt` 的版本——注入层这行忘了删。
- **造成的状况**：每次审批前端收到两条 approval_result：第一条（无 sub）让 `addSub(undefined)` 崩掉、结论显示一次；第二条再显示一次——**审批结论重复渲染**且控制台报错。
- **最终效果**：危险操作确认是安全机制的展示窗口，恰恰不能渲染出错。
- **解决**：注入层只负责"阻塞等按钮"，事件发射权全部收归内核循环；注释里写明"XX 已由内核 emit，此处不再重复发"。

### 54. SSE 长静默期被掐断：运行活着，实时视图死了（"断流不等于停跑"）
- **容易忽略**：工具执行（fetch_js 起无头浏览器渲染，几十秒）和模型调用期间 SSE **一个字节都不发**——空闲连接在 WebView/代理眼里和死连接没区别；且前端 onerror 一次就永久放弃。
- **造成的状况**：实测第 7 步 fetch_js 渲染时前端断流，用户看到"实时连接中断"以为运行停了；事后查事件存档：运行一路跑到第 10 步正常结题（model_done，8,408 字报告）——**报告一直都在，只是实时通道断了没送到屏幕**。用户由此误判"阈值触发后不出报告了"。
- **最终效果**：CLI 时代报告直接打印到终端，没有网络层可断；上 Web 后多了 SSE 这层传输，传输层的问题被误读成生成层的退化。
- **解决**：三层防御——① 服务端空闲 15s 发 SSE 注释帧 `: ping`（浏览器忽略但保活，17s 静默实测发出 2 帧）；② 服务端支持 `?since=N` 断点续传；③ 前端 onerror 不再一次放弃，自动重连最多 5 次（退避 0.8s×n），带 since 只取增量不重放，5 连败才降级为提示"从历史记录查看"。**诊断口诀**：先查 runs.jsonl 有没有收尾记录——有 = 传输层问题，没有 = 内核问题，别搞反方向。
