# realize.md —— Agent 系统实现文档（大节点 × 小节点全景）

> **定位**：AGENT_NODES.md 是"学习手册"（按学习顺序讲原理），本文档是"实现图纸"（按运行时架构讲系统）。
> **格式**：每个大节点 = 做什么 / 目标 / 技术 / 作用（产品价值） / 与下一节点的衔接 / 注意事项；大节点下的小节点 = 做什么 / 怎么做 / 作用与目标 / 衔接 / 注意。
> **视角**：产品经理——每个大节点先回答"它替产品解决了什么问题"。
> 细节佐证见代码与 PITFALLS.md（标注 ⚠P 编号对应踩坑清单条目）。

---

## 第〇部分：系统全景——一个问题是怎么流过整个系统的

```
用户提问
   │
   ▼
[1 配置接入层]──拿key连模型        [6 记忆系统]──注入历史偏好
   │                                   │
   ▼                                   ▼
[2 消息状态层]◄──组装 system+memory+user
   │
   ▼
┌─[4 Agent Loop]────────────────────────────────────┐
│  每一轮：                                          │
│   [1.3 模型调用]→模型决策                          │
│      ├─点菜→[3 工具系统]执行→[2.2 回填]→下一轮      │
│      │        （危险工具→[7.2 HITL]人工确认）        │
│      └─不点菜→输出最终报告，循环结束                 │
│  [5 上下文工程] 每轮检查：超限就压缩                 │
│  [7.3 预算] 每轮记账：80%预警/100%强停              │
└────────────────┬──────────────────────────────────┘
                 ▼
[6 记忆] 提取偏好存档    [11 可观测性] 记录运行日志
                 │
   （大任务模式下）[8 多智能体] 拆题→并行worker→汇总
   （评测模式下）  [10 评测系统] judge打分→对比报告
   （扩展模式）    [12 MCP] 工具动态接入外部生态
```

**产品视角总纲**：1/2 层是"通话能力"，3 层是"动手能力"，4 层是"自主能力"，5/6 层是"记忆能力"，7 层是"安全带"，8 层是"组织能力"，9/10 层是"质量与度量"，11 层是"仪表盘"，12 层是"生态接入"。**前四层构成最小可用 agent，后面每一层都是规模化前的补强。**

---

## 大节点 1：配置与模型接入层

- **做什么**：把"连哪个模型、怎么调用、调用时发生了什么"封装成全项目唯一的入口。
- **目标**：换模型零改代码；所有 LLM 调用可观测、可审计。
- **技术**：OpenAI 兼容协议（Chat Completions）；`python-dotenv` 管理密钥；pydantic 对象转 dict；JSONL 追加写。
- **作用（产品价值）**：模型供应商是可替换组件——今天用 DeepSeek，明天换 GLM，产品逻辑不动。这是"供应商锁定风险"的对冲。
- **与下一节点衔接**：本层产出的 `call_llm()` 是所有上层（循环/多智能体/评测）的唯一武器；它操作的 messages 结构由大节点 2 定义。
- **注意事项**：⚠P34 统一入口才能统一观测——绕过 `call_llm` 直接调 SDK 的代码 = 观测盲区（Stage 6 实测 token 计量为 0）。

### 小节点 1.1 环境配置（config.py）
- **做什么**：集中管理模型三要素（API_KEY/BASE_URL/MODEL）+ 全局参数（最大循环步数、结果截断长度、压缩阈值、保留轮数）。
- **怎么做**：`os.getenv` 读 `.env`；参数全部带默认值。
- **作用与目标**：配置与逻辑分离；调参（如压缩阈值）不用碰业务代码。
- **衔接**：被所有模块 import。
- **注意**：key 永不进代码/仓库（.gitignore）；模型必须支持 tool calling。

### 小节点 1.2 客户端工厂（make_client）
- **做什么**：校验配置并返回 OpenAI client 实例。
- **怎么做**：缺 key 直接抛中文错误提示（fail fast）。
- **注意**：错误提示要可行动——直接告诉用户"复制 .env.example 为 .env 填三要素"。

### 小节点 1.3 模型调用（call_llm）★全项目最核心的 10 行
- **做什么**：接收 messages + tools（+ 任意 extra 参数透传），发请求，返回（assistant 消息，token 用量）。
- **怎么做**：参数白名单组装；支持 `**extra`（temperature/response_format 等透传）——这是 Stage 6 修复计量盲区的产物。
- **作用与目标**：所有模型调用的唯一通道；RECORDER 挂在门上，进门即留档。
- **衔接**：上层全部只认识它；下层是 HTTP 与协议细节。
- **注意**：⚠P34；extra 参数原样透传，不白名单不拦截（灵活性）。

### 小节点 1.4 调用录制（CallRecorder）
- **做什么**：每次调用发生时，把当时的请求（messages+tools 的**深拷贝**）和完整响应定格存档。
- **怎么做**：`copy.deepcopy` 防"引用污染"（Stage 9 实测：三次捕获全是最后一次）；pydantic 对象就地转 dict。
- **作用与目标**：capture_full_io.py 的数据源；事后可逐字段回放模型"当时看到的世界"。
- **注意**：⚠P36 快照必须深拷贝——messages 是引用，loop 会继续往里 append。

---

## 大节点 2：消息状态层（messages 协议）

- **做什么**：定义并维护 agent 的唯一状态载体——一个按时间追加的消息数组。
- **目标**：让失忆的模型每轮都能"回忆"起任务全貌。
- **技术**：四种角色（system/user/assistant/tool）+ tool_call_id 配对 + 全量重发协议。
- **作用（产品价值）**：状态 = agent 的记忆本体。理解它，就理解了 agent 为什么"记得"（历史在数组里）和为什么"忘事"（历史被压缩/裁掉了）。
- **与下一节点衔接**：tools 参数（大节点 3）是 messages 之外随请求重发的"能力清单"；两者合起来才是完整请求。
- **注意事项**：⚠P1/P2——assistant 消息必须回填、tool 消息必须靠 id 严格配对，缺一 400。

### 小节点 2.1 四种角色
- **做什么**：system（规则/人设/记忆，只出现一次在最前）、user（任务）、assistant（模型决策，含 tool_calls 与 reasoning_content）、tool（工具事实）。
- **怎么做**：严格按时间顺序 append；system 稳定放最前（利于缓存命中省钱）。
- **注意**：模型每轮失忆，数组里没有的它就不知道——"状态缺失 = 行为异常"的总根源。

### 小节点 2.2 结果回填
- **做什么**：工具执行后，把结果封装成 `{"role":"tool","tool_call_id":<id>,"content":<字符串>}` 追加。
- **怎么做**：逐个 tool_call 对应一条 tool 消息；assistant 消息先于 tool 消息回填。
- **注意**：⚠P1/P2——少一条、id 配错，轻则 400，重则模型张冠李戴地自信输出错误结论（静默失败更危险）。

### 小节点 2.3 消息序列化（serialize）
- **做什么**：把 SDK 的 pydantic 对象转成纯 dict/JSON，供 trace 存档与日志。
- **注意**：⚠P4 蛇形命名（input_schema/reasoning_content）——先 model_dump 再操作。

---

## 大节点 3：工具系统（tools.py）

- **做什么**：定义 agent 能做什么（声明）、怎么做（实现）、怎么选（分发），并提供搜索/抓取/笔记三类真实能力。
- **目标**：给失忆只会说话的模型装上"手"；同时控制每件"手"返回的信息体量。
- **技术**：JSON Schema 声明 + 注册表模式（REGISTRY 字典）+ ddgs 搜索 + httpx/trafilatura 抓取 + 线程本地存储。
- **作用（产品价值）**：工具质量 = agent 能力上限。工具设计是 PM 最能直接发力的环节（给非人类用户做产品设计）。
- **与下一节点衔接**：工具执行结果经大节点 5 的裁剪后进入 messages，由大节点 4 的循环驱动"调用→回填"。
- **注意事项**：⚠P7 注册表有 ≠ 模型知道（三处一致：实现→REGISTRY→TOOLS 清单）；⚠P8 描述措辞即行为；⚠P12 并行放大限流。

### 小节点 3.1 工具声明（TOOLS / REAL_TOOLS）
- **做什么**：每个工具一张"菜单卡"：name + description（何时用/何时不用/边界）+ parameters（JSON Schema，enum 锁值）。
- **怎么做**：静态数组，随每次请求原样重发。
- **作用与目标**：模型对工具的全部认知来源；描述质量直接决定调用质量。
- **衔接**：TOOLS 是发给模型的；REGISTRY 是给自己代码的，两者通过工具名关联。
- **注意**：⚠P8 描述一句话的增删会改变行为——改描述=改行为，必须跑评测回归。

### 小节点 3.2 注册表与分发器（REGISTRY / dispatch）
- **做什么**：`{工具名: 函数}` 查表执行；未知名返回可行动的错误（"工具不存在，可用工具是…"）。
- **怎么做**：模型给的 arguments 是 JSON 字符串 → json.loads → 传参调用。
- **作用与目标**：agent 唯一的"手"；执行权完全在你，模型碰不到。
- **衔接**：生产版的 dispatch 前面插一道 HITL 检查（大节点 7.2）、后面留一个 MCP 转发分支（大节点 12）。
- **注意**：执行异常要捕获并转成"可行动"的错误文本回填，不能让异常裸奔进上下文。

### 小节点 3.3 搜索工具（web_search）
- **做什么**：关键词 → 前 5 条结果的 title/url/snippet。
- **怎么做**：双通道——配置了 TAVILY_API_KEY 时优先走 Tavily（质量高/快），失败或未配置降级免费 ddgs（重试+退避 3 次）；结果字段精选 + 截断 + "已截断"附注。
- **注意**：免费源超时/限流是本项目最大痛点（实测单次搜索 65 秒、多 worker 被打崩），生产强烈建议配 Tavily/博查。
- **作用与目标**：agent 的眼睛；失败信息可行动（教模型换关键词或改用 fetch_url 直接访问官网）。
- **衔接**：产物进 messages；同时被 05 阶段的证据收集器截留（供 judge 核对有据性）。
- **注意**：免费源限流与中文站点反爬是数据源天花板，生产换 Tavily/博查只需改这一个函数。

### 小节点 3.4 网页抓取（fetch_url）
- **做什么**：URL → 正文纯文本（截断至 4000 字符）。
- **怎么做**：httpx 下载（UA/超时/跟随重定向）+ trafilatura 正文抽取 + 三层校验（非 http 前缀/下载失败/无正文各有可行动报错）。
- **作用与目标**：agent 的深度阅读能力；正文抽取 = 进门压缩第一道（去广告导航噪音）。
- **注意**：登录墙/纯前端渲染的站点抓不到——如实报错让模型换路，不伪造内容。

### 小节点 3.5 笔记工具（note_write / note_read）
- **做什么**：上下文之外的持久笔记：追加写入带时间戳的发现 / 读回全文。
- **怎么做**：纯文件读写；路径用 **thread-local** 存储（`set_notes_file`）——多智能体并行时每个 worker 一份独立笔记。
- **作用与目标**：外置记忆（agentic memory）；与压缩配套——压缩让上下文短，笔记保证事实不丢（Stage 4 实测无笔记则反复重搜）。
- **衔接**：使用时机由各 agent 的 system prompt 规则驱动（查证完就写、收尾前必读）；Stage 8 的跨会话记忆复用同一模式。
- **注意**：并行必须 thread-local；写入时机靠 prompt 教，模型偶尔偷懒——生产可升级为强制检查点。

### 小节点 3.7 JS 渲染抓取（fetch_js）
- **做什么**：Playwright 无头浏览器渲染页面（滚动触发懒加载）后提取全部可见文本，补上 fetch_url 的"JS 页盲区"。
- **怎么做**：chromium headless → goto → 等待渲染 → 滚动三遍触发懒加载 → 回顶 → inner_text("body")（含页脚）→ 截断回填；用默认浏览器 UA（实测怪异 UA 会让网站 JS 走不同渲染分支，目标内容整个不出现）。
- **作用与目标**：与 fetch_url 分工明确（描述里写清"静态用 fetch_url，JS 页/内容过少才用本工具，更慢"），实测从"查不到"变成"完整价格表"。
- **衔接**：失败/成功语义与 fetch_url 一致（可行动错误 + 截断附注）。
- **注意**：⚠P41（JS 壳 + 懒加载 + UA 分支）；需安装 playwright + chromium。

### 小节点 3.6 危险工具示例（send_report）
- **做什么**：模拟"把报告发到邮箱"的对外动作（MVP 只 mock 不真发）。
- **作用与目标**：给 HITL 机制一个真实拦截对象；演示"对外发送"类动作的确认流程。

---

## 大节点 4：Agent Loop（循环控制器）

- **做什么**：把"模型决策→执行工具→结果回填"自动循环，直到任务完成或触达安全阀。
- **目标**：流程控制权交给模型（它决定下一步），控制权留给代码（你决定能不能停）。
- **技术**：for 循环 + 两个停止条件 + 死线注入 + 逐步打印。
- **作用（产品价值）**：这一层是 agent 与聊天机器人的分界线。**油门（下一步干什么）交给模型，刹车（最大步数/预算/死线）握在代码手里**——这是整个项目最重要的产品架构决策。
- **与下一节点衔接**：循环中每轮产生的上下文膨胀，由大节点 5 接管；循环执行的工具来自大节点 3；循环的每一次调用走大节点 1。
- **注意事项**：⚠P13 agent 不会自己收敛（死线注入是刚需）；⚠P14 强停必须优雅收尾；⚠P15 循环严格串行同步。

### 小节点 4.1 主循环体
- **做什么**：调模型→无 tool_calls 则返回（停止A）→ 有则逐个执行→逐个回填→下一轮。
- **怎么做**：严格执行"点几道菜、全部执行、全部回填、才能下一轮"的节拍。
- **注意**：多个 tool_calls 用内层 for 逐条处理，不并行执行（MVP 简化）。

### 小节点 4.2 停止条件（双出口）
- **做什么**：出口 A=模型不再点菜（finish_reason=stop，它认为完成）；出口 B=步数耗尽（强制，config.MAX_LOOP_STEPS）。
- **注意**：⚠P5 判断"完成"要看 stop 而非仅"无 tool_calls"（防 length 截断误判）；出口 B 必须优雅收尾（见 7.4）。

### 小节点 4.3 死线注入（收敛护栏）
- **做什么**：倒数第 2 步时，往 messages 追加一条 user 消息："⚠️时间将尽，立即 note_read 并输出报告，不要再搜索。"
- **怎么做**：runtime 在循环里判断 `step == max_steps - 1`。
- **作用与目标**：**agent 不会自己收敛，"永远还差一点"是默认行为**——收敛必须由确定性代码管理（Stage 5 实测两次步数耗尽，注入后 7 步收敛）。
- **注意**：这是"模型负责聪明、代码负责兜底"哲学的标准实现；子 agent 循环同样要装（Stage 7 首跑全体罢工的教训）。

### 小节点 4.4 过程可视性（show_step / 逐步打印）
- **做什么**：每轮打印步数、点菜内容、token 消耗。
- **作用与目标**：没有它，调优就是盲人摸象（Stage 5 步数耗尽问题靠加打印才定位）。
- **注意**：print 到终端只是最小形态，正式版是大节点 11 的 RunLog。

---

## 大节点 5：上下文工程（context.py + 工具内裁剪）

- **做什么**：管理模型每一轮"看到什么（编排）、看多少（压缩）、怎么看（形式）"。
- **目标**：维护准确的"当前状态"，用最小高信号 token 集合对抗三重敌人——成本（每轮全量重发）、退化（context rot）、干扰（噪音稀释注意力）。
- **技术**：字段精选/硬截断（进门裁剪）；LLM 摘要重建历史（compaction）；安全切分算法。
- **作用（产品价值）**：决定 agent 的成本曲线与长任务可行性。没有它，15 步任务 = 几十万 token + 模型失忆；有了它，同任务 token 被"按住"在几千级。
- **与下一节点衔接**：裁剪后的事实进笔记（大节点 6 前身）；压缩不能替代笔记——两者配套（压缩管短、笔记管不丢）。
- **注意事项**：⚠P17 切分点避开 tool 消息（孤儿→400）；⚠P18 不造 assistant 消息（缺 reasoning_content 被拒收）；⚠P19 有损压缩的代价是反复重搜。

### 小节点 5.1 进门裁剪（在 tools.py 内，四步流水线）
- **做什么**：正文抽取（trafilatura 去噪音）→ 字段精选（只留 title/url/snippet）→ 硬截断（snippet 300 / 整体 4000）→ 截断附注。
- **怎么做**：纯代码，零成本，工具返回的那一刻执行。
- **作用与目标**：第一道防线——4 万字网页进门只剩 4 千字符。
- **衔接**：被裁剪的结果进入 messages；多轮之后成为"旧工具输出"，交由 compaction 清理。
- **注意**：截断附注是安全带（模型知道是残卷）；对外部工具用"兜底截断+trace 观测画字段画像"。

### 小节点 5.2 历史压缩（compact_messages）
- **做什么**：旧历史 → 一次 LLM 摘要（保留任务目标/关键发现/未完成/约束四类）→ 与最近 6 条原文重建上下文。
- **怎么做**：三步——**规范化**（pydantic 转 dict，⚠P 同族 bug）→ **安全切分**（切分点向后滑动避开 tool 孤儿）→ **摘要重建**（只注入 user 摘要消息，不造 assistant）。
- **作用与目标**：把"滚雪球"变成"恒温器"：实测 26 步任务触发 13 次压缩，单轮输入被按在几千 token。
- **衔接**：触发器在 agent loop 里（见 5.3）；被压掉的事实靠笔记兜底。
- **注意**：摘要有损——没笔记时的表现是"反复重搜"；摘要提示词的保留清单是效果关键。

### 小节点 5.3 触发器（maybe_compact / 循环内检查）
- **做什么**：每轮调用后看真实的 `usage.prompt_tokens`，超过 config.MAX_CONTEXT_TOKENS 就压缩。
- **怎么做**：用计费真值判断（不用估算）；触发线也可兼作预算预警位。
- **注意**：压缩发生在调用之后——单轮工具结果本身超胖时峰值仍会冒头，要配合进门裁剪（5.1）。

---

## 大节点 6：记忆系统（memory.py）

- **做什么**：把"关于用户本人的稳定偏好"跨会话持久化，新会话自动注入。
- **目标**：agent 越用越懂你。与笔记的边界是生命线：**笔记=任务内研究发现（用完即弃），记忆=用户偏好（长期有效）**——两者混装是记忆系统头号设计错误。
- **技术**：JSON 文件存储 + LLM 提取（JSON mode）+ 规则去重合并 + 容量上限。
- **作用（产品价值）**：个性化留存。"上次说过用中文+表格对比"，这次不用再说。
- **与下一节点衔接**：记忆文本注入 system prompt（大节点 2 的 system 角色）；提取/合并是一次普通 LLM 调用（走大节点 1）。
- **注意事项**：⚠P24 提取环节必须有回显；错误偏好需要用户可编辑接口（生产）。

### 小节点 6.1 存储与注入（MemoryStore / format_for_prompt）
- **做什么**：读写 memory.json（facts 列表，每条 text+ts）；会话启动时把记忆渲染成"【用户长期记忆】…"注入 system。
- **怎么做**：空记忆返回空串（不占 token）；只取最近 20 条。
- **衔接**：注入发生在 agent 启动、组装 system 之前；位置稳定利于缓存。

### 小节点 6.2 偏好提取（extract_preferences）
- **做什么**：会话结束时，一次 LLM 调用从 transcript 里抽偏好。
- **怎么做**：提示词硬性划界——只收用户本人的稳定偏好；**调研内容、临时要求（"今天发给某人"）严禁混入**；JSON mode 输出。
- **注意**：⚠P24 必须回显"提取到 N 条候选"——首跑静默返回空，无法排查。

### 小节点 6.3 合并（merge）
- **做什么**：新偏好入库：相同/包含关系跳过；上限 20 条丢最旧。
- **注意**：MVP 用文本规则判重；进阶应让 LLM 做语义合并与冲突更新（"用户改主意了"场景）。

---

## 大节点 7：安全件（guardrails.py）

- **做什么**：预算记账与收尾、危险工具分级与人工确认。
- **目标**：回答"agent 出错时，谁来兜底、代价多小"。
- **技术**：真实计费值记账；终端 y/n（生产换审批系统）；分级注册表。
- **作用（产品价值）**：三件套分工——**预算兜"跑飞了"，HITL 兜"做不该做的事"，记忆兜"理解错偏好"**。三件齐了，demo 才敢变产品。
- **与下一节点衔接**：HITL/预算都嵌在大节点 4 的循环里；护栏对 MCP 工具自动延伸（大节点 12）。
- **注意事项**：⚠P30 只拦危险动作（少而准，防批准疲劳）；⚠P31 拒绝回执话术决定后续行为；⚠P33 收尾调用本身耗 token，触发线设 80%。

### 小节点 7.1 危险分级（DANGEROUS_TOOLS）
- **做什么**：注册表 `{工具名: 危险原因}`；判断标准五类——不可逆/对外发送/花钱/改权限/低置信。
- **衔接**：MCP 外部工具接入时，写类自动扫描升级进这张表。

### 小节点 7.2 人工确认（ask_human）
- **做什么**：sensitive 工具执行前，终端展示工具名/参数/危险原因，y/n 决定执行。
- **怎么做**：**无论批准还是拒绝，回执文本都作为 tool 结果喂回模型**——拒绝回执固定话术："不要重复尝试，改为直接输出内容"（实测模型完全服从）。
- **注意**：MVP 用 input()；生产换 IM/工单异步审批，"暂停-询问-回填"骨架不变。

### 小节点 7.3 预算（Budget）
- **做什么**：累计 usage.total_tokens；三水位——`near_limit(0.8)` 软提醒、`near_limit(0.95)` **硬收尾（拔工具箱：最后一次调用不传 tools，模型物理上无法再点菜）**、`exhausted` 强停；三档 stop_reason 分别记账（budget_hard_stop / budget_exhausted）。
- **注意**：用 API 计费真值记账，不估算；软提醒实测会被模型无视（三次全越线），硬收尾才是保证——物理约束 > 行为请求；验证：2967/3000 优雅结题。

### 小节点 7.4 优雅收尾（WRAP_UP_MSG）
- **做什么**：预算 80% 时注入死线消息——停止调研，note_read 后输出《阶段性结题报告》：已确认发现+未完成清单+续跑建议。
- **作用与目标**：**强停 ≠ 报错**。实测 LOW_BUDGET 下自动产出结题报告。

---

## 大节点 8：多智能体（orchestrator.py + sub_agent.py）

- **做什么**：主 agent 拆题、派工，多个子 agent 并行调研，各回传浓缩结论，主 agent 汇总。
- **目标**：两大收益——**并行省时**（实测 2.5x）与**上下文隔离**（子 agent 读几万 token 只回传 ≤700 字，主 agent 永远轻装）。
- **技术**：LLM 拆题（JSON mode）+ ThreadPoolExecutor 并行 + thread-local 笔记 + 错峰启动。
- **作用（产品价值）**：单 agent 的"上下文肥胖"用**组织分工**解决；它是"100 个工具怎么编排"的终极答案（分层：调度员只拿部门级工具）。
- **与下一节点衔接**：多智能体产出与单 agent 同台进评测（大节点 10）打擂台；每个子 agent 内部就是一个大节点 4 的循环。
- **注意事项**：⚠P13 子 agent 同样需要死线护栏（首跑全员罢工）；⚠P26 并行共享全局状态用 thread-local；⚠P27 任务书自包含不重叠；⚠P28 能不拆就不拆。

### 小节点 8.1 拆题（decompose）
- **做什么**：一次 LLM 调用把问题拆成 1~4 份**自包含**任务书（目标+查证点+边界），附拆分策略。
- **怎么做**：JSON mode；提示词硬性规定"≤4 份、任务自包含（调查员看不到用户原话）、范围不重叠、简单问题只给 1 份"。
- **注意**：规划器会贪多（把大纲写成完美主义清单）——数量限制必须写进 prompt；拆分质量决定收敛性。

### 小节点 8.2 并行派工（dispatch）
- **做什么**：线程池并行跑 worker；错峰启动（每个延迟 5 秒）缓解搜索限流；统计墙钟 vs 串行（实测加速 2.5x）。
- **注意**：worker 异常要捕获成"失败报告"而非让线程崩掉拖垮全局。

### 小节点 8.3 子 agent（run_sub_agent）
- **做什么**：领任务书→独立循环调研（搜索/抓取/笔记/压缩/死线一应俱全）→输出**浓缩结论**（≤300 字要点+来源，"未查到"如实标注）。
- **怎么做**：任务书注入 system；启动时 set_notes_file 设独立笔记；无拆题权（防套娃）。
- **作用与目标**：上下文隔离的执行者——读几万 token，只交几百字。
- **注意**：死线护栏是出厂标配（Stage 7 首跑全体不交卷的教训）。

### 小节点 8.4 汇总（synthesize）
- **做什么**：主 agent 一次调用，综合各调查员结论 → 最终对比报告（保留来源、冲突如实指出）。
- **注意**：system 必须写"调查员标'未查到'的项如实说明，不许脑补"——实测子 agent 全军覆没时，主 agent 输出"本次调研失败，请勿用于决策"，诚实验收沿责任链传导。

---

## 大节点 9：结构化输出与规划（research_agent_v3.py）

- **做什么**：三段式交付——Plan（JSON 研究大纲）→ Execute（按大纲调研）→ Report（带 [n](url) 引用的报告）。
- **目标**：从"自由发挥"到"按合同交付"：输出可解析、过程不漏项、结论可核验。
- **技术**：`response_format=json_object`（JSON mode 强制合法 JSON）；大纲注入 system；引用格式约束。
- **作用（产品价值）**：规划是 workflow 思维（固定骨架），调研是 agent 思维（自由执行）——**混血模式**：结构保证覆盖度，放权保证适应性。
- **与下一节点衔接**：产出的答案+证据进大节点 10 的评测；大纲是 judge"覆盖度"维度的依据。
- **注意事项**：⚠P13 收敛护栏（死线注入）在此首次发明；规划器贪多需限制子问题数量；JSON mode 下 prompt 仍要说清要什么结构。

### 小节点 9.1 规划（plan）
- **做什么**：独立 LLM 调用产出 `{"goal","subquests":[{"id","q","what_to_verify"}],"constraints"}`。
- **注意**：子问题 ≤3、一句话描述——大纲必须"10 步内可完成"，否则任务永远做不完。

### 小节点 9.2 执行（research）
- **做什么**：大纲进 system，复用循环（工具+笔记+压缩+死线），逐步打印决策过程。
- **注意**：过程可视性是调试前提（Stage 5 靠它发现不收敛）。

### 小节点 9.3 报告（Report）
- **做什么**：收尾前强制 note_read，按笔记输出，每个事实带 [n](url)。
- **作用**：引用让交付物可核验，也是 judge"sourced"维度的评分依据。

---

## 大节点 10：评测系统（evals/）

- **做什么**：12 道真实研究题（factual/comparison/open）+ LLM-as-judge 评分器 + 一键运行器。
- **目标**：让每次改动都有分数可对比——agent 的回归测试。**没有 eval 的迭代 = 没有验收标准的 PRD。**
- **技术**：题库 JSON；judge 单模型按四维 rubric（有据/有源/覆盖/简洁）打 0~1 分，JSON mode 输出；运行器串行跑题聚合报告。
- **作用（产品价值）**：把"感觉变好了"变成"分数从 0.57 到 0.73"；Stage 6 三方选型、Stage 7 多智能体验证全靠它。
- **与下一节点衔接**：评测分数进可观测面板（大节点 11）；groundedness 依赖的证据来自 v3 运行时的截留。
- **注意事项**：groundedness 是灵魂（judge 拿 agent 的原始证据核对结论，首跑就抓到编造价格）；单 judge 优于多 judge；**单题噪声大，结论看多题均值**（⚠P36）。

### 小节点 10.1 题库（questions.json）
- **做什么**：12 题，含 id/题型/question/key_points（评分参考要点）。
- **注意**：题目小而具体（5-12 步可完成）、从真实需求来。

### 小节点 10.2 评分器（judge.py）
- **做什么**：输入问题+大纲+证据+答案 → 输出四子分+总分+扣分原因（JSON）。
- **注意**：**groundedness 设计**——judge 不上网查证，拿 agent 调研时的原始证据核对"结论是否被支撑"；judge 也可能误判，低分题要人工复核。

### 小节点 10.3 运行器（run_eval.py）
- **做什么**：`--n 3` 快跑 / 全量跑；逐题跑 agent→judge→聚合 Markdown 报告。
- **注意**：改动前后各跑一遍对比——这是回归测试的完整闭环。

---

## 大节点 11：可观测性（observability.py + dashboard.py + classify_failures.py）

- **做什么**：所有运行统一落一条结构化 JSONL；LLM 归类失败模式；生成静态观测面板。
- **目标**：三个问题随时可答——成功率多少？token 花在哪？失败归哪几类？
- **技术**：JSONL 追加写；LLM 归类（JSON mode，固定类目）；纯 HTML/CSS 面板（无依赖）。
- **作用（产品价值）**：agent 上线后 PM 的日常 = 看数据、归类失败、驱动改进（相当于增长 PM 看漏斗）。本项目多次事故（记忆静默失败、worker 集体罢工、计量为 0）的教训总集。
- **与下一节点衔接**：RunLog 是全系统唯一运行数据源，所有实现（含未来的）都应接入。
- **注意事项**：⚠P34 统一入口；归类提示词要教边界（"审批拒绝是正常拦截不算故障"）；坏行跳过不吞错。

### 小节点 11.1 运行日志（log_run / load_runs）
- **做什么**：每次运行结束追加一行 JSON（run_id/时间/实现/问题/步数/工具序列/tokens/预算/stop_reason/errors/extra）。
- **注意**：写入时深拷贝/规范化（沿用 Stage 9 教训）；坏行容错。

### 小节点 11.2 失败归类（classify_failures.py）
- **做什么**：筛出非正常收尾或带错误的运行，LLM 归入固定类目，输出分布表+逐条依据。
- **注意**：类目固定才可聚合；"审批拒绝"与"工具失败"要区分（一个是正常拦截，一个是故障）。

### 小节点 11.3 观测面板（dashboard.py → observatory/index.html）
- **做什么**：四指标卡（总运行/正常收尾率/累计 token/均值）+ 失败分布条形图 + 运行历史表。
- **注意**：合格标准三问——成功率？token 花在哪？失败归哪几类？答不出就是没做完。

---

## 大节点 12：MCP 接入（mcp_client.py）

- **做什么**：让 agent 作为 MCP Host 接入外部 MCP server，动态发现并调用外部工具。
- **目标**：工具从"自己写死"升级为"生态动态接入"——接一个 server = 多一组工具，核心代码零改动。
- **技术**：官方 `mcp` Python SDK；后台线程跑 asyncio 事件循环维持 stdio 会话常驻；`run_coroutine_threadsafe` 提供同步接口；工具名加前缀防冲突；pydantic→OpenAI tools 格式转换。
- **作用（产品价值）**：把 agent 接进工具生态（文件系统/Slack/数据库……）而不被任何框架绑架。**MCP 只标准化工具的发现/描述/调用，不碰循环与上下文——agent 主权不变。**
- **与下一节点衔接**：MCP 工具合并进 tools 参数后，与本地工具在同一条循环里无差别使用；写类工具自动进大节点 7 的危险分级。
- **注意事项**：⚠P4 pydantic 蛇形命名（input_schema）；anyio TaskGroup 包裹异常必须打 traceback；**外部工具默认不可信**——写类自动升级危险级走 HITL。
- **配置（Stage 11 补）**：`MCP_SERVERS` 环境变量（JSON 数组，每项 name/command/args）> 旧开关 `MCP_FS=1`（默认 filesystem 指向项目目录）> 不启用；三级解析在 config.py，agent_core 支持**多 server 并挂**（clients 列表 + 前缀路由 + 危险分级全量扫描）。

### 小节点 12.1 会话管理（start / _main / _submit）
- **做什么**：后台线程启动事件循环；stdio 连接 server→initialize→list_tools→常驻保活；主线程同步提交协程。
- **注意**：会话不能关（`while True: sleep`保活）；启动超时要报错（fail fast）；错误必须带完整 traceback（TaskGroup 会包住真异常）。

### 小节点 12.2 工具发现与转换（openai_tools）
- **做什么**：`list_tools()` 结果 → 每个工具转 `{"type":"function","function":{name(加前缀),description,parameters}}`。
- **作用与目标**：**MCP 的 inputSchema 就是 JSON Schema**——和你手写的菜单卡同源，"换信封"即可。
- **注意**：⚠P4 蛇形命名；前缀命名空间防与本地工具重名。

### 小节点 12.3 调用转发（call_tool / owns）
- **做什么**：dispatch 遇到带前缀的工具名 → 剥前缀 → `session.call_tool` → 拼接文本内容回填。
- **注意**：返回的 content 是块列表，逐块取 text。

### 小节点 12.4 安全联动（危险分级自动延伸）
- **做什么**：接入时扫描工具名，含 write/edit/move/create 自动注册进 DANGEROUS_TOOLS。
- **作用与目标**：**接入生态的同时，安全件自动延伸到生态**——外部工具默认不可信。

---

## 大节点 13：辅助设施（学习与调试工具）

### 小节点 13.1 交互实验室（playground/）
- **做什么**：FastAPI 透明代理 + 表单页面：构造任意角色/工具/参数的请求，回显实际请求体 + 原始响应（含思考模式开关、格式化输出三面板）。
- **作用与目标**：把"发请求"变成交互实验；报错原文也是教材（developer 角色 400、max_tokens 截断都在这里亲眼见过）。
- **注意**：key 走后端 .env 不经浏览器；透明原则——不做任何解析纠错。

### 小节点 13.2 完整输入输出捕获（capture_full_io.py）
- **做什么**：挂 RECORDER 跑一次 agent，逐次导出每次调用的完整请求/响应 Markdown，文末附自动校验（快照递增/id 配对/finish_reason/token 复核四项）。
- **作用与目标**：学习材料生成器 + 深度调试器。
- **注意**：⚠P36 深拷贝快照（引用污染教训的产物）。

### 小节点 13.3 数据资产（traces/ notes/ logs/ observatory/）
- **做什么**：traces 存每次运行完整消息流；notes 存学习复盘与输入输出存档；logs 存 RunLog 与失败报告；observatory 存面板。
- **注意**：traces 内容不进 git（.gitignore），结构与学习价值在内部分享时展示。

---

## 附一：大节点依赖关系速查

```
1 配置接入 ──► 2 消息状态 ──► 4 Agent Loop ──► 8 多智能体（4 的组织化）
                  ▲               │                │
3 工具系统 ───────┘               ▼                ▼
（循环的手）                 5 上下文工程      10 评测（打分选型）
（循环的大脑燃料）           6 记忆系统
（循环的刹车）               7 安全件 ──► 12 MCP（护栏自动延伸）
                                  │
                                  ▼
                             11 可观测性（一切的数据源与仪表盘）
9 结构化输出 = 2+3+4+5 的组合模式
```

## 附二：产品视角的一句话总结（每层对应一个产品决策）

| 大节点 | 对应的产品决策 |
|---|---|
| 1 配置接入 | 供应商不锁定，模型可替换 |
| 2 消息状态 | 状态设计 = 产品记忆的设计 |
| 3 工具系统 | 给非人类用户做产品设计（工具=功能） |
| 4 Agent Loop | 放权多少、刹车在哪 |
| 5 上下文工程 | 成本曲线与长任务可行性 |
| 6 记忆 | 个性化留存与遗忘策略 |
| 7 安全件 | 出错时谁来兜底、代价多小 |
| 8 多智能体 | 能力上限 vs 复杂度成本 |
| 9 结构化输出 | 交付物契约化、可核验 |
| 10 评测 | 先建度量再做选择 |
| 11 可观测 | 上线后的日常运营抓手 |
| 12 MCP | 自建生态 vs 接入生态 |

---

## 大节点 14：研究工作台 GUI（webapp/）

- **做什么**：把 agent 从命令行搬进浏览器——输入问题，实时观看执行流（点菜/工具结果/压缩/预算事件），拿到带引用的报告；含审批按钮、中止按钮、运行历史。
- **目标**：三件产品化——过程可视性（透明原则从开发者特权变用户权利）、控制权（批准/拒绝/中止/预算变成按钮）、资产入口（历史/报告可回看）。
- **技术**：FastAPI + **SSE**（Server-Sent Events）事件流 + 原生 HTML/JS（延续不上框架路线）；核心是**事件化改造**——`common/agent_core.py` 把循环里两个写死的 IO 口抽成可注入接口：`emit`（输出口：CLI 注入 print / Web 注入 SSE 队列）、`approver`（审批口：CLI 注入 input / Web 注入"网页按钮+POST"，线程阻塞在 threading.Event 上等用户点击）、`stop_check`（中止口：查按钮状态）。
- **作用（产品价值）**：agent 从脚本变产品的临门一脚。CLI 与 GUI 是同一个内核的两种"脸"——注入 CLI 实现即终端版，注入 Web 实现即工作台版，**决策逻辑/工具/提示词/护栏全部原样**（回归验证：改造前后评测与 RunLog 数据一致）。
- **与下一节点衔接**：审批端点（POST /approve）是 HITL 的 Web 化；历史列表直接读 RunLog（大节点 11）；面板与工作台可合并为同一控制台。
- **注意事项**：后台线程阻塞等审批（threading.Event + POST 回填）沿用 MCP 客户端练过的模式；重复 emit 要去重（实测 approval_request 发了两次）；线程对象必须在路由回填给 agent（SSE 生成器靠它判断运行是否结束——曾因未回填直接 AttributeError）。

### 小节点 14.1 SSE 事件流（/api/research/{id}/events）
- **做什么**：生成器轮询事件队列，把新事件以 `data: {...}\n\n` 推给浏览器；线程结束且事件发完时推 done 并收流。
- **注意**：生成器判断结束依赖 `agent._thread`——路由启动线程后必须回填（实测缺失即 AttributeError 断流）。

### 小节点 14.2 审批（/approve + 前端横幅）
- **做什么**：approval_request 事件到达 → 前端弹出批准/拒绝横幅 → POST 回填 → 阻塞的 approver 放行 → 循环继续。
- **作用**：Stage 8 的"暂停-询问-回填"骨架的 Web 化，拒绝回执话术原样保留。

### 小节点 14.3 中止（/stop + ⏹按钮）
- **做什么**：点击设置 stop 标志 → 循环下一轮检测到 → 注入收尾指令（复用 7.4 的 WRAP_UP_MSG）→ 优雅结题。
- **作用与目标**：用户随时能踩刹车，且停得体面（阶段性结题报告而非白屏）。

### 小节点 14.4 运行历史（/api/runs + 左栏）
- **做什么**：读 RunLog 倒序渲染历史列表（问题/状态/token），点击可复制问题重跑。
- **衔接**：数据源就是大节点 11 的 logs/runs.jsonl。

### 小节点 14.5 层级编号执行流（步 → 编号颗粒 → 详单）
- **做什么**：执行流按"步"分组成卡片，步内每个动作自动编号为 `步.序`（1.1 模型调用 → 1.2 工具执行 → 1.3 回填确认…；0 号段为启动准备），每颗颗粒带用时、可展开详单（输入/输出/token明细/参数/完整结果）；压缩颗粒额外展开目的/方法/统计/摘要全文。
- **怎么做**：后端 `_emit_sub()` 在循环内自动分配 `step.sub` 编号（每步重置）；新增 `backfill`（回填确认）与 `inject`（死线/预算/熔断注入）颗粒事件；前端按编号前缀分组渲染 `<details>` + 编号列表。
- **作用与目标**：颗粒度细到"每个动作一行、一行一个编号"，学习时能逐颗对照 realize.md 的节点定义。
- **衔接**：编号由 agent_core 循环结构决定（与 realize.md 各小节一一对应）。
- **注意**：审批结果（approval_result）沿用请求颗粒的编号；启动段编号固定 0.x。

### 小节点 14.6 可下钻执行流（详单 + 计时）
- **做什么**：执行流里每个节点卡片可点击展开"详单"——本步输入（消息总数/token明细：prompt/completion/思考/缓存命中）、模型输出（content + 点菜单 JSON）、每个工具的输入参数与完整输出（前4000字符）、各环节用时（模型调用 ms/每个工具 ms/总耗时）；压缩卡片展开显示**压缩目的、压缩方法、压缩统计（旧条数/保留条数/摘要字符/前后长度）与本次摘要全文**。
- **怎么做**：后端在 agent_core 循环内加计时器（模型调用/每个工具分别计时），事件负载携带 detail 字段（模型输出 dict + token 明细）与 full_result/args/summary/stats；compact_messages 返回值升级为三元组（messages, summary, stats），全部调用点同步更新；前端卡片用 `<details>` 展开。
- **作用与目标**：把"过程可视性"升级为"可下钻学习视图"——每个节点能回答：输入是什么、输出是什么、花了多久、（压缩时）怎么压的为什么压。
- **衔接**：数据源就是 agent_core 循环内的计时器与 compact_messages 的 stats。
- **注意**：详单数据会让 SSE 事件变大（单工具完整结果 4000 字符）——本地单用户可接受，多用户部署要考虑降采样。
