
## 20:22:30
子问题1（模型清单）— 来源：api-docs.deepseek.com/quick_start/pricing + /api/list-models/（官方）。抓取日期：2026年（页面引用 2026-09-14 相关说明）。
当前可用模型标识（GET /models 返回）：deepseek-flash、deepseek-v4-pro（owned_by: deepseek）。
- deepseek-flash：模型版本 DeepSeek-V4.1-Flash；支持非思考+思考模式（默认思考模式）；上下文长度 1M；最大输出 384K；支持 Vision；并发上限 2500。
- deepseek-v4-pro：模型版本 DeepSeek-V4-Pro-0813；支持非思考+思考模式（默认思考）；上下文 1M；最大输出 384K；不支持 Vision；并发上限 500。
- 已弃用/遗留名称：deepseek-v4-flash 和 deepseek-v4-flash-vision-exp 仍被接受，但对应模型已退役(retired)，请求由 DeepSeek-V4.1-Flash 服务并按 Flash 价格计费。
- 两者均支持 Json Output、Tool Calls、Responses API、Anthropic API、Chat Prefix Completion(Beta)、FIM Completion(Beta，仅非思考模式)。
- Base URL(OpenAI格式)：https://api.deepseek.com ；Anthropic格式：https://api.deepseek.com/anthropic

## 20:22:30
子问题2（定价）— 来源：api-docs.deepseek.com/quick_start/pricing（官方），单位：每 1M tokens，币种：美元 USD。抓取日期见笔记。分高峰(PEAK)/低谷(OFF-PEAK)：
deepseek-flash：
- 输入(缓存命中)：off-peak $0.003 / peak $0.006
- 输入(缓存未命中)：off-peak $0.15 / peak $0.3
- 输出：off-peak $0.6 / peak $1.2
deepseek-v4-pro：
- 输入(缓存命中)：off-peak $0.022 / peak $0.044
- 输入(缓存未命中)：off-peak $0.66 / peak $1.32
- 输出：off-peak $1.98 / peak $3.96
规则：低谷价=高峰价的一半。高峰时段：01:00-04:00 与 06:00-10:00 UTC，周一至周五；其他时间为低谷。计费=token数×单价，优先扣减赠送余额。
补充：官方注明 V4 Pro 将于 2026-09-14 后继续提供 API 服务，计费方式不变。

## 20:23:27
[子问题3-更新日志] 官方 Change Log (api-docs.deepseek.com/updates)：
- 2026-09-10：正式发布 DeepSeek-V4.1-Flash（新架构家族最小模型，原生多模态视觉理解）。API 模型名改为 deepseek-flash 即调用最新 V4.1 Flash。上一代 V4 Flash 与 V4 Flash Vision Exp 已退役；为兼容，deepseek-v4-flash 与 deepseek-v4-flash-vision-exp 模型名临时路由到 V4.1 Flash。同时"API pricing adjustment"宣布价格相应下调。
- 2026-09-14 相关：官方决定在 2026-09-14 之后继续提供 DeepSeek V4 Pro 的 API 服务，计费方式不变。
- 2026-08-21：发布实验性多模态模型 DeepSeek-V4-Flash-Vision-Exp（model='deepseek-v4-flash-vision-exp'）。
- 2026-08-13：DeepSeek-V4-Pro GA 正式发布（APP/Web/API），模型名 deepseek-v4-pro。来源域名：api-docs.deepseek.com

## 20:23:37
[子问题1&2-模型与定价] 官方 Models & Pricing 页 (api-docs.deepseek.com/quick_start/pricing)，抓取日期 2026-09-14 后：
当前官方模型（模型标识）：
- deepseek-flash → 版本 DeepSeek-V4.1-Flash。支持非思考+思考模式（默认思考）；上下文 1M；最大输出 384K；支持 Json Output / Tool Calls / Responses API / Anthropic API / Chat Prefix Completion(Beta) / FIM(Beta,仅非思考)；支持 Vision(✓)。并发上限 2500。旧名 deepseek-v4-flash、deepseek-v4-flash-vision-exp 仍可接受但对应模型已退役，请求由 V4.1-Flash 服务并按 Flash 价格计费。
- deepseek-v4-pro → 版本 DeepSeek-V4-Pro-0813。支持非思考+思考模式（默认思考）；上下文 1M；最大输出 384K；同上功能但 Vision 不支持。并发上限 500。
Base URL：OpenAI 格式 https://api.deepseek.com；Anthropic 格式 https://api.deepseek.com/anthropic

定价（单位：每 1M tokens，币种 USD）：
deepseek-flash：输入缓存命中 off-peak $0.003 / peak $0.006；输入缓存未命中 off-peak $0.15 / peak $0.3；输出 off-peak $0.6 / peak $1.2。
deepseek-v4-pro：输入缓存命中 off-peak $0.022 / peak $0.044；输入缓存未命中 off-peak $0.66 / peak $1.32；输出 off-peak $1.98 / peak $3.96。
时段规则：off-peak 价为 peak 价的一半。Peak 时段为 UTC 周一至周五 01:00-04:00 与 06:00-10:00（其余为 off-peak）。
来源域名：api-docs.deepseek.com
