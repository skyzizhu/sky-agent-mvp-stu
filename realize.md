# realize.md —— Agent 系统实现文档（大节点 × 小节点全景 v2）

> **定位**：AGENT_NODES.md 是"学习手册"（按学习顺序讲原理），本文档是"实现图纸"（按运行时架构讲系统）。
> **格式**：每个大节点 = 做什么 / 目标 / 技术 / 作用（产品价值） / 与下一节点的衔接 / 注意事项；小节点同构。
> **视角**：产品经理——每个大节点先回答"它替产品解决了什么问题"。
> **覆盖承诺**：从用户提问到最终报告生成，全链路 16 个大节点、70+ 小节点，与代码一一对应；事故与解法标注 ⚠P 编号（对应 PITFALLS.md）。

---

### 第〇部分：系统全景——一个问题是怎么流过整个系统的

```
用户在工作台提问（可选档位：快问2万/标准5万/深度18万）
   │
   ▼
[15 工作台前端] ──POST /api/research──► [15.1 Web内核注入]
   │                                      │
   ▼                                      ▼
[1 配置接入层]──连模型    [6 记忆]──注入偏好   [13 MCP]──按配置挂外部工具
   │
   ▼
┌─[4 执行内核 agent_core]──────────────────────────────────────────────┐
│  [4.0 JSON大纲规划(plan)] ──生成目标与子问题，注入System Prompt       │
│  每一轮（严格串行）：                                                   │
│   [4.4 死线/预算注入] → [1.3 模型调用] → 模型决策                        │
│     ├─危险工具→[7.2 HITL]人工确认                                       │
│     ├─搜索调用→[3.8 搜索去重与缓存(SessionToolCache)] (精确/模糊拦截引导) │
│     ├─网页抓取→[5.4 单页Map即时萃取] (长网页提炼核心事实，压缩比80%+)     │
│     ├─普通工具→[3 工具系统]执行→[5.1 进门裁剪]→回填(编号颗粒)            │
│     ├─连败→[4.5 熔断器]  上下文超限→[5.2 历史压缩(全额计入预算)]        │
│     ├─95%预算/步数耗尽→[4.6 硬收尾：拔工具+摘未应答调用+DSML清洗+笔记兜底]│
│     └─无工具调用→最终报告，循环结束                                         │
└─────────────────────────────────┬────────────────────────────────────┘
                                  ▼
[6 记忆] 提取偏好存档    [12 可观测] RunLog+探针 (记录saved_calls)
                                  │
   （深度任务）[8 多智能体] 拆题→并行worker(独立缓存与萃取)→汇总
   （质量验收）[11 评测] judge打分(比对outline)   （选型）[10 模式对比]
   （历史回看）[15.5 事件存档→详情回放]
```

**产品视角总纲**：1/2 层"通话能力"，3 层"动手能力"，4 层"自主能力+安全带"，5/6 层"记忆能力"，7 层"安全带"，8 层"组织能力"，9/10/11 层"质量-度量-运营"，12 层"生态接入"，13 层"调试工具"，14 层"方法论沉淀"，15 层"产品门面"。**1-4 层构成最小可用 agent，其后每层都是规模化前的补强。**

---

## 大节点 1：配置与模型接入层

- **做什么**：把"连哪个模型、怎么调用、调用时发生了什么"封装成全项目唯一入口。
- **目标**：换模型零改代码；所有 LLM 调用可观测、可审计。
- **技术**：OpenAI 兼容协议；python-dotenv；pydantic→dict 序列化；JSONL 追加写。
- **作用（产品价值）**：模型供应商是可替换组件——供应商锁定风险的对冲。
- **与下一节点衔接**：`call_llm()` 是所有上层唯一武器；messages 结构由大节点 2 定义。
- **注意事项**：⚠P34 统一入口才能统一观测——绕过 `call_llm` 直连 SDK = 观测盲区。

### 小节点 1.1 环境配置（config.py）
- **做什么**：集中管理模型三要素 + 全局参数：最大循环步数、结果截断长度、压缩阈值/保留轮数、**预算档位**（BUDGET_TIERS：quick 2万/standard 5万/deep 18万）、**TAVILY_API_KEY**（搜索源）、**MCP_SERVERS**（外部工具，三级解析：env JSON > MCP_FS=1 > 不启用）、**DEBUG_DUMP**（结构诊断开关）。
- **怎么做**：os.getenv + 默认值；JSON 配置解析。
- **作用与目标**：把"换模型/调行为"变成改配置不改代码——供应商不锁定的落点；所有全局阈值（步数/截断/压缩/预算档位）集中一处，学习时改一个数字就能观察 agent 行为的差异。
- **注意**：key 永不进代码/仓库；模型必须支持 tool calling。

### 小节点 1.2 客户端工厂（make_client）
- **做什么**：校验配置并返回 OpenAI client 实例。
- **怎么做**：缺 key 直接抛可行动错误（fail fast，提示"复制 .env.example 填三要素"）。
- **作用与目标**：全项目唯一的 client 出口——超时/重试/key 校验策略一处生效；fail fast 把"配置缺失"拦截在第 0 秒，而不是拖到第三次调用时才爆。

### 小节点 1.3 模型调用（call_llm）★全项目最核心的 10 行
- **做什么**：接收 messages + tools（+ `**extra` 透传任意参数），发请求，返回（assistant 消息，token 用量）。
- **作用与目标**：所有模型调用的唯一通道；RECORDER 观测钩子挂在门上，进门即留档。
- **衔接**：上层（循环/多智能体/评测/压缩）全部只认识它。
- **注意**：⚠P34——Stage 6 实测绕过它直连 SDK 导致对比实验 token 计量为 0。

### 小节点 1.4 调用录制（CallRecorder）
- **做什么**：每次调用发生时，把当时的请求（messages+tools **深拷贝**）与完整响应定格存档；pydantic 就地转 dict。
- **作用与目标**：capture_full_io 的数据源；事后可逐字段回放模型"当时看到的世界"。
- **注意**：⚠P36 messages 是引用，不深拷贝则所有记录"变成最后一次"。

---

## 大节点 2：消息状态层（messages 协议）

- **做什么**：定义并维护 agent 的唯一状态载体——按时间追加的消息数组。
- **目标**：让失忆的模型每轮"回忆"起任务全貌。
- **技术**：四种角色（system/user/assistant/tool）+ tool_call_id 配对 + 全量重发协议。
- **作用（产品价值）**：状态 = agent 的记忆本体。agent 一切行为异常，本质都是"当前状态不完整或被污染"。
- **与下一节点衔接**：tools 参数（大节点 3）与 messages 并列构成完整请求。
- **注意事项**：⚠P1/P2——assistant(tool_calls) 必须回填、tool 消息必须按 id 严格配对，缺一 400。

### 小节点 2.1 四种角色
- system（规则+记忆注入，稳定放最前利于缓存）/ user（任务）/ assistant（决策，含 tool_calls 与 reasoning_content）/ tool（事实）。
- 注意：数组里没有的，模型就不知道——"状态缺失 = 行为异常"总根源。
- **作用与目标**：messages 数组就是 agent 的全部记忆与状态——分清四种角色各自的职责（谁定规则、谁下任务、谁做决策、谁供事实），是读懂"模型为什么这么回"和上手上下文工程（大节点 5）的地基。

### 小节点 2.2 结果回填与配对
- **做什么**：每条 tool_call 执行后，回填 `{"role":"tool","tool_call_id":<id>,"content":<字符串>}`。
- **作用与目标**：让模型的每次"点菜"都有回音——id 配对回填是工具调用协议的闭环；做对，模型才知道"我要的信息拿到了"；做错，则满盘皆输且无报错。
- **注意**：⚠P2 id 错配 = 静默失败（模型张冠李戴地自信输出错误结论），比 400 更危险。

### 小节点 2.3 消息序列化（serialize）
- **做什么**：pydantic 对象 → 纯 dict/JSON（trace 与事件详单用）。
- **作用与目标**：把内存对象变成可存档/可回放/可对比的纯文本——trace、事件详单、评测复盘全靠它；也是学习"API 真实传输长什么样"的素材来源。
- **注意**：⚠P4 蛇形命名（input_schema/reasoning_content）——本家族已踩 4 次。

---

## 大节点 3：工具系统（tools.py）

- **做什么**：定义 agent 能做什么（声明）、怎么做（实现）、怎么选（分发），提供搜索/抓取/JS渲染/笔记四类真实能力。
- **目标**：给失忆只会说话的模型装上"手"，并控制每只"手"返回的信息体量。
- **技术**：JSON Schema 声明 + 注册表分发 + Tavily/ddgs 双通道搜索 + httpx/trafilatura 抓取 + Playwright JS 渲染 + 线程本地笔记。
- **作用（产品价值）**：工具质量 = agent 能力上限；工具设计是 PM 最能直接发力的环节（给非人类用户做产品设计）。
- **与下一节点衔接**：执行结果进门裁剪后进入 messages，由大节点 4 循环驱动"调用→回填"。
- **注意事项**：⚠P7 注册表有 ≠ 模型知道；⚠P8 描述措辞即行为；⚠P12 并行放大限流；⚠P41/42 JS 页与列表嵌套。

### 小节点 3.1 工具声明（TOOLS / REAL_TOOLS / NOTE_TOOLS / FETCH_JS_TOOL）
- **做什么**：每工具一张菜单卡：name + description（何时用/何时不用/边界）+ parameters（JSON Schema，enum 锁值）。
- **作用与目标**：菜单卡就是模型的"产品界面"——description 写什么，模型就怎么用；工具质量的上限在这一层决定，是"给非人类用户做产品设计"的核心实践场。
- **注意**：⚠P8 描述一句话增删即改变行为，改动必须跑评测回归；**列表合并必须用 extend 而非 append**（⚠P42：append 会嵌套成 tools[2]=[...] → API 400，实测事故）。

### 小节点 3.2 注册表与分发器（REGISTRY / dispatch）
- **做什么**：`{工具名: 函数}` 查表执行；未知名返回可行动错误（"工具不存在，可用工具是…"）。
- **生产架构改造**：
  - **实例级注册表隔离**：内核持有 `self.tools_registry = dict(tools_mod.REAL_REGISTRY)` 浅拷贝实例，杜绝全局 `REGISTRY.clear()` 引发的并发竞态与脏污染。
  - **分发管线扩展**：`dispatch` 透传 `cache`（会话缓存）、`extract_client`（萃取模型）、`goal`（课题目标）与 `on_extract_usage`（预算回调）。
  - **元数据返回**：由单纯返回字符串升级为 `(result_str, meta_dict)` 元组，携带耗时、缓存命中及萃取提纯指标，为可观测性提供原始数据。
- **衔接**：生产版 dispatch 前有 HITL 检查（大节点 7.2）、中有搜索去重与网页单页萃取（小节点 3.8 / 5.4）、后有 MCP 转发分支（大节点 13）。- **作用与目标**：工具系统的"总调度台"——模型点菜后的一切（安全检查/缓存/萃取/MCP 转发/异常转译）都在这一站发生；以后要往循环里加横切能力，唯一改造点就是它。
- **注意**：执行异常要转成可行动错误文本回填，不能让异常裸奔进上下文。

### 小节点 3.3 搜索工具（web_search）——双通道与摘要优先
- **做什么**：关键词 → 前 5 条 title/url/snippet。
- **怎么做**：配置 TAVILY_API_KEY 时优先 Tavily（质量高/快），失败或未配置**降级免费 ddgs**（重试+退避 3 次：2s/4s）；字段精选 + 截断 + "已截断"附注。
- **摘要优先原则（Snippet-First）**：工具描述与 System Prompt 明确引导——若搜索结果返回的 snippet 已清晰包含事实、具体数字、版本号或定义，优先直接调用 `note_write` 沉淀并保留对应 `url` 编号，无需盲目打开每一个网页，显著降低长程调研中的网络 I/O 与上下文负担。
- **作用与目标**：agent 的眼睛。免费源超时/限流是本项目最大痛点（实测单次搜索 65 秒、多 worker 被打崩）——生产强烈建议配 Tavily/博查。
- **注意**：错误信息可行动（教模型换关键词或改用 fetch_url 直访官网）。

### 小节点 3.4 网页抓取（fetch_url）——静态抽取与 SPA 自动透明降级
- **做什么**：URL → 正文纯文本（截断 4000 字符）。
- **怎么做**：
  1. 静态快速通道：httpx（现代浏览器 UA/超时/重定向）+ trafilatura 正文抽取；
  2. **SPA 智能探测与无缝降级（Auto-Fallback to `fetch_js`）**：若静态提取正文为空或短小（$< 150$ 字符，或命中 "enable javascript", "正在加载" 等 SPA 骨架屏特征），底层**自动透明降级调用 `fetch_js` 无头浏览器执行渲染与滚动懒加载提取**。成功后返回正文并附注 `"(静态抓取无正文/SPA骨架屏，已自动降级为无头浏览器JS渲染获取)"`，前端点亮紫色徽章 `🌐 自动降级JS渲染`，模型零感知且不浪费额外轮次。
  3. 容灾退出：若两者均无有效内容，返回结构化错误并建议基于搜索摘要或更换其他链接。
- **作用与目标**：把网页变成"人话版正文"（去导航/去广告/留数据），是证据质量的第一道关；SPA 自动降级让模型不必"学会判断要不要 JS 渲染"——复杂性对模型透明。

### 小节点 3.5 JS 渲染抓取（fetch_js）
- **做什么**：Playwright 无头浏览器渲染页面（滚动三遍触发懒加载）后提取**含页脚**的全部可见文本。
- **怎么做**：chromium headless → goto(load) → 等待渲染（wait_ms 可调）→ 滚动+回顶 → inner_text("body") → 截断回填；**用默认浏览器 UA**。
- **作用与目标**：补 fetch_url 的"JS 页盲区"——实测 yourtools.xyz 从"查不到定价"到"完整价格表 ¥12/¥28/¥98"。
- **注意**：⚠P41 三连坑——① JS 壳页面静态抓取必然空（三层诊断法：查原始HTML关键词→查可见文本占比→查JS文件）；② **懒加载必须滚动触发**（定价区滚动前后差 13 行数据）；③ **怪异自定义 UA 会让网站 JS 走不同渲染分支**（目标内容整个不出现）——用默认浏览器 UA。与 fetch_url 分工写进双方描述（"静态内容过少时优先用本工具重抓同一网址"）。

### 小节点 3.6 笔记工具（note_write / note_read）——外置记忆的读写接口
- **双工具分工**：
  - **note_write = 存档 + 核销 二合一**：① 关键发现**追加写入磁盘**（sessions/{sid}/notes.md）——磁盘不受任何压缩影响；② 内容命中「子问题 X」标记时自动核销看板（sync_checklist_from_note），一石二鸟；
  - **note_read = 收拢 + 成文前置**：写报告前把散落十几轮历史的全部发现一次性收拢到模型眼前。
- **外置记忆生命周期（与压缩/收尾的完整串行关系）**：

  ```
  抓网页 → 萃取发现 → note_write 写入磁盘
      ↓ 上下文膨胀
  压缩：丢弃「过程」（搜索词/网页原文/中间推理），摘要保留「结论」
      ↓ 结论的安全保证：永远在两个地方之一——当前上下文 或 磁盘笔记
  收尾两条取回路径：
      ① 模型调 note_read 读回全量笔记（正常收尾，模型主动取）
      ② 系统把笔记全文直接嵌入结题指令（硬收尾，系统强制喂，无需模型动作）
      ↓
  报告 = 笔记的加工产物（两条路径殊途同归）
  ```

- **作用与目标**：① 对抗压缩——上下文有损、磁盘无损，**事实永远在「上下文或磁盘」之一**，不存在丢失态；② 报告的原材料——最终报告主要基于笔记写成，极端情况笔记直接兜底成交付物；③ 上下文经济学——贵的原始网页信息萃取后可安全从上下文丢弃，成本大幅下降。
- **怎么做**：纯文件读写；路径 thread-local（`set_notes_file`）——多智能体并行时每个 worker 独立笔记；note_read 读回有截断保护（极端超长由硬收尾嵌入路径覆盖）。
- **衔接**：使用时机由 system prompt 驱动（查证完就写、收尾前必读）；与压缩（5.2）构成双保险——压缩管「上下文瘦身」，笔记管「事实不丢」；Stage 8 跨会话记忆复用同一模式。
- **注意**：⚠P19 无笔记时模型反复重搜——写入时机靠 prompt 教，模型偶尔偷懒，生产可升级为强制检查点。

### 小节点 3.7 邮件投递（已按 MVP 聚焦原则移除，存档于 git 历史）
- **曾经的实现**：模型经 `request_send` 工具自主判断并登记投递意图（零副作用），报告定稿后由独立服务 `services/`（mail_service + report_delivery）带真实内容预览请求人工确认再发送——完整走通过真实 SMTP（126 邮箱）端到端。
- **为什么移除**：Deep research agent 的本质是研究循环（规划→检索→笔记→压缩→报告），邮件是外部 I/O 集成而非研究能力；留在主链路会让工具箱/提示词/宿主线程都背着从不工作的代码，稀释学习焦点。
- **沉淀下来的学习要点**（细节见 git 历史 commit `30eb723`/`b90ce58`/`a37103f` 及 PITFALLS P54）：① **判断权在模型，执行权在代码**——agent 与工作流的分界线；② 对外不可撤回动作必须有人工确认闸门（7.2 的思想在任何宿主层都适用）；③ 外部能力放服务层，与内核边界用注入函数划清；④ 纯发送类请求曾误入研究循环烧穿预算——意图路由是宿主的职责。

### 小节点 3.8 会话级工具缓存与去重（SessionToolCache，common/cache.py）
- **做什么**：拦截重复或高度相似的搜索查询，阻断重复拉取已抓取过的网页，并实现域名级失败反爬感知。
- **怎么做**：
  - **精准命中（Exact Match）**：查询词规范化（`query.strip().lower()`）直接命中缓存字典，0 毫秒立即返回已有搜索结果并计入 `saved_calls`。
  - **模糊去重与引导建议（Fuzzy Match & Advisory Steering）**：通过中英文分词计算 Jaccard 相似度（并集覆盖比）与 Overlap 包含重合系数。若 $\max(\text{Jaccard}, \text{Overlap}) \ge 0.75$，判定语义高度重叠，返回历史结果并注入引导附注：`[系统提示：该搜索与已有查询高度重合...]`，引导模型查阅已有笔记或更换全新角度，阻断无效原地重搜。
  - **网页防重拉取（URL Caching）**：规范化 URL（去除尾斜杠与大小写归一），命中直接读取内存正文，免除重复网络 HTTP 请求与无头浏览器昂贵的渲染开销，彻底杜绝目标站频控 429。
  - **域名级失败与反爬感知（Domain Failure Perception）**：记录各域名的抓取失败次数。当同一域名累计失败 $\ge 2$ 次（如反爬 403/强需登录），后续发生失败时附带引导建议：`💡【系统提示：该域名已多次抓取失败】...`，引导模型不要反复撞墙，优先基于搜索摘要或切换第三方信源。
  - **收益可观测**：提供 `get_stats()` 统计节约的外部调用量（`saved_calls`），并在 Web 界面呈现 `⚡ 命中缓存/免请求` 徽章。
- **作用与目标**：解决模型长程决策时注意力漂移导致的"反复重搜同一关键词"通病，显著降低端到端耗时与 API 账单成本。
- **注意**：模糊匹配绝不能粗暴抛错中断，必须采用温和引导话术；缓存生命周期绑定单次 Agent 会话，多智能体 worker 各持独立实例，保证无状态任务间的绝对干净隔离。

---

## 大节点 4：执行内核与循环（agent_core.py）★全系统心脏

- **做什么**：把 Stage 2-8 验证过的生产循环封装成**可注入内核** `ResearchAgent`：决策流程/工具/提示词/护栏零逻辑改动，仅三个 IO 口可注入——`emit`（输出口：CLI=print / Web=SSE 队列）、`approver`（审批口：CLI=input / Web=网页按钮）、`stop_check`（中止口）。
- **目标**：CLI 与 GUI 是同一内核的两种"脸"；**逻辑零改动**由评测回归证明。
- **技术**：依赖注入；JSON 大纲规划；`_emit_sub` 层级编号；`_trim_unanswered_tool_calls` 协议修复；失败熔断器；硬收尾；DSML 标签清洗；笔记兜底。
- **作用（产品价值）**：**油门（下一步干什么）交给模型，刹车（步数/预算/死线/熔断）握在代码手里**——整个项目最重要的产品架构决策。
- **与下一节点衔接**：循环中每轮的上下文膨胀交大节点 5；安全事件交大节点 7；每次运行落大节点 12 的 RunLog；Web 形态由大节点 15 驱动。
- **注意事项**：⚠P13 agent 不会自己收敛；⚠P15 循环严格串行同步；⚠P40 注入绝不能插在 tool_calls 与 tool 结果之间；⚠P44 软性收尾挡不住超支。

### 小节点 4.0 JSON 大纲规划与动态任务核销看板（plan 节点 + Dynamic Todo Checklist）
- **做什么**：主循环启动前的合同制定与长程调研中的动态任务状态机。
  1. **制定合同**：调用 LLM 生成结构化研究大纲（包含主课题 `goal`、2-3 个分解子问题 `sub_questions`、查证要点 `what_to_verify`、约束 `constraints`）。
  2. **动态看板化**：运行时从大纲实例化 `checklist` 状态机（每项记录 `id`, `q`, `what_to_verify`, `status: pending|in_progress|completed`, `summary`）。
  3. **双轨核销机制**：
     - **显式工具核销**：提供 `update_checklist(sub_id, status, summary)` 工具，模型查清事实并记下笔记后调用，更新状态并附上简短事实结论；
     - **笔记感知自动核销（Heuristic Auto-Sync）**：模型调用 `note_write` 沉淀含 `【子问题X】` 发现时，运行时自动将对应子问题核销为 `completed` 并抽取第一句作为依据，实现双重兜底。
  4. **逐轮节拍动态刷新**：在主循环每一步的 `[系统进度]` user 提示词末尾，动态拼入最新看板状态（`[x] 已完成` / `[~] 正在查证` / `[ ] 待查证`），模型时刻清楚进度。
  5. **全量核销自发收尾门禁**：一旦所有子问题均核销为 `completed`，运行时在下一轮立即注入强提示：“🎉 所有大纲子问题已全部核销完成！核心事实已充分，请立即 note_read 并输出完整报告，严禁继续发散检索”，促使模型自发收尾交卷。
- **怎么做**：`client.chat.completions.create` + `response_format={"type": "json_object"}`；planner **不带 tools 参数**（只拆题不看工具箱，规划-执行分离）；所耗 token 实时计入 `budget`；产生 `action="plan"` 与 `action="checklist"` 事件推送前端/CLI；**plan 事件带与 step 同构的全量详单**（①完整输入 messages ②`model_dump()` 全字段输出 ③token 明细含 reasoning/cached），Web 端 0.3 卡片可逐字段下钻；将大纲动态拼入后续主循环的 `system_prompt` 中，并作为最终返回的 `outline` 资产供后续评测比对。
- **作用与目标**：解决模型长程探索中"最初定的大纲被后续海量网页信息冲淡而遗忘"的执行脱节问题；让模型从"盲目漫游"变成"手持清单逐项打勾核销"，从根本上保障调研逻辑聚焦与自发收敛。
- **注意**：规划消耗 token 必须记账；大纲解析需兼容容错（JSON 解析失败时平滑降级）；核销更新需触发实时事件推送。

### 小节点 4.1 可注入内核（ResearchAgent）
- **做什么**：构造时接收 emit/approver/stop_check/budget_max/use_mcp/impl/**session**；`run(question)` 执行完整研究并返回结果（answer/stop_reason/steps/tokens/errors/outline/saved_calls）。`session` 注入多轮研究会话（大节点 16）：启动注入历史上下文，结束保存摘要与发现，RunLog `extra.session_id` 溯源。
- **作用与目标**：一套决策逻辑跑遍 CLI/Web/评测三种宿主——改内核只改一处，所有形态同时受益；也是"框架即依赖注入"的活教材（对比 LangGraph 的感受会非常直接）。
- **注意**：⚠P36 类事故预防——所有事件数据在 emit 时深拷贝/规范化。

### 小节点 4.2 主循环体（串行节拍）
- **做什么**：调模型→无 tool_calls 则返回（停止A）→有则逐个执行→逐个回填→下一轮。
- **作用与目标**：agent 的心脏节拍——把"调模型→执行→回填→下一轮"跑直，后面所有护栏（熔断/死线/压缩/收尾）都是往这个节拍上挂的零件；节拍不直，护栏全歪。
- **注意**：多 tool_calls 用内层 for 逐条处理；执行异常转可行动错误文本，不让异常裸奔进上下文。

### 小节点 4.3 停止条件（六出口）
- **做什么**：stop（模型完成）/ budget_hard_stop（95% 硬线）/ budget_exhausted / user_stop（⏹ 按钮）/ max_steps / error——**任何出口都必须有交付物**：max_steps 出口同样触发强制无工具收尾（4.6），占位符时代已终结。
- **作用与目标**：回答"什么时候停、停了交付什么"——六出口保证任何情况下用户拿到的是交付物而不是半句话；这是 agent "可交付"的底线设计。
- **注意**：⚠P5 判断"完成"要看 stop 而非仅"无 tool_calls"（防 length 截断误判）；死线注入条件为 `step >= max-1`（最后两步每步都提醒）。

### 小节点 4.4 阶梯式巡航自检与充分度收敛门禁（收敛护栏）
- **做什么**：
  1. **充分度收敛门禁（System Prompt 硬性规则）**：主 Agent 与多智能体 Worker 同步注入——"深度研究不等于无休止穷举！对照用户问题，只要核心主干要点（定位/链路/入口/边界等）已有事实支撑（以笔记为准），即判定为信息充分；严禁继续发散检索无关的边缘配置、分值折算、详细价目表等次要细节，立即 note_read 并输出报告，坚决杜绝为了'追求极致完备'而原地漫游"。
  2. **阶梯式巡航自检提醒（动态注入）**：
     - **早期（step < 4 且 预算 < 50%）**：系统静默，给模型充足空间进行广度发散与深读；
     - **中期（step >= 4 或 预算 >= 50%）**：进度状态注入 `· [进展自检] 若核心主干事实已充分，请立即收尾出报告，避免发散到非核心细节`，促使模型在主干齐备后主动踩刹车；
     - **晚期（预算 >= 80%）**：注入 `· ⚠️ 预算偏紧：请收敛调研范围，优先补关键缺口并准备结题`；
     - **死线（倒数第 2 步，step >= max - 1）**：强注入 `⚠️ 时间将尽：note_read 后立即输出报告，不要再搜索`。
- **作用与目标**：⚠P13 **agent 不会自己收敛**——收敛是 runtime 的职责；模型往往陷入"检索到 A 想要查 A.1，查到 A.1 想要查 A.1.1"的无限发散黑洞。阶梯提醒与充分度门禁结合，让模型在第 3-5 步主干完备时即可从容结题，避免把步数和预算拖到 95% 硬收尾。

### 小节点 4.5 失败熔断器
- **做什么**：连续 4 次工具结果以"错误"开头 → 注入强制改道指令（"禁止再用相同方式尝试，基于已有信息输出阶段性结论"），最多 2 次；成功即清零。
- **作用与目标**：⚠P39 实测为找一个不存在的定价 35 次调用烧穿预算——**证据获取不到时模型不会主动放弃**，熔断器替它止损。

### 小节点 4.6 硬收尾：拔工具 + 摘除未应答tool_calls + 磁盘笔记嵌入 + DSML清洗 + 原文兜底
- **做什么**：预算 95% 或耗尽时，执行四重确定性防御：
  1. **协议修复（`_trim_unanswered_tool_calls`）**：摘除尾部"已声明调用但未执行"的 assistant 消息，保证 OpenAPI 协议合法，彻底杜绝后续请求 400（⚠P40 变体）。
  2. **磁盘笔记全文注入（`_build_forced_final_msg`）**：从本地磁盘直接读取 `NOTES.md` 原文嵌入结题 prompt，告知模型"直接基于以下已有笔记整理报告，严禁输出 `<｜｜DSML｜｜...>` 标签，严禁发起工具调用"。模型无需也不能再调 `note_read`。
  3. **无工具物理收尾**：发起 `tools=None` 的最终调用，模型物理上无法再调用工具。
  4. **DSML 标签净化（`_clean_report`）**：使用正则彻底剔除 DeepSeek 等模型因强制拔工具可能误喷出的 `<｜｜DSML｜｜ calls>...` 假调用语法，保障输出文本干净。
  5. **坏报告检验与磁盘笔记兜底（`_is_invalid_report`）**：若最终模型吐出空文本或残存 DSML 代码，系统自动将磁盘 `NOTES.md` 作为《研究笔记（预算强制收尾汇总）》兜底输出；代码中设置 `t_fin = 0.0` 初始化守卫，杜绝未绑定变量异常。
- **作用与目标**：⚠P44 软性收尾指令实测三次全被无视（61581/60000、4563/3000、60588/60000）；硬收尾实测 2967/3000 优雅结题。**物理约束 > 行为请求**——在极端异常和超支时确保成果绝不丢失。

### 小节点 4.7 层级编号事件（_emit_sub）
- **做什么**：循环内事件自动编号 `步.序`（1.1 模型调用 → 1.2 工具 → … → 回填确认）；每步重置；启动段为 0.x（0.1 为大纲规划）；新增 backfill（回填确认）与 inject/breaker（护栏注入）颗粒。
- **作用与目标**：GUI 层级视图的数据基础；编号与 realize.md 小节一一对应。
- **注意**：approval_result 沿用 approval_request 的编号；熔断/注入绝不能插在 tool_calls 与 tool 结果之间（⚠P40）。

### 小节点 4.8 过程可视性（CLI 渲染）
- **做什么**：默认 emit 渲染成终端输出（与 Stage 8 体验一致）；Web 模式由大节点 15 注入 SSE 实现。
- **作用与目标**：让每一次模型决策在终端可见——调试 agent 的第一入口；Web 版（大节点 15）本质只是换了个渲染器，事件源相同。
- **注意**：⚠P24 不可观测的环节坏了都不知道——每个环节都要有回显。

---

## 大节点 5：上下文工程（context.py + 工具内裁剪）

- **做什么**：管理模型每一轮"看到什么（编排）、看多少（压缩）、怎么看（形式）"。
- **目标**：维护准确的"当前状态"，用最小高信号 token 集合对抗三重敌人——成本（每轮全量重发）、退化（context rot）、干扰（噪音稀释注意力）。
- **技术**：进门四步裁剪；LLM 摘要重建历史（stats 含 summarizer_input/output 全量 IO）；安全切分算法。
- **作用（产品价值）**：决定成本曲线与长任务可行性。没有它，15 步任务 = 几十万 token + 模型失忆；有了它，token 被按在几千级。
- **与下一节点衔接**：被压掉的事实靠大节点 6 前身（笔记）兜底——**压缩与笔记是配套能力，不是二选一**。
- **注意事项**：⚠P17 切分点避开 tool 消息（孤儿→400）；⚠P19 无笔记时压缩的代价是反复重搜；摘要必须保留四类信息（目标/发现/待办/约束）。

### 小节点 5.1 进门裁剪（tools.py 内四步流水线 + 头尾保留）
- **做什么**：正文抽取（trafilatura 去噪音）→ 字段精选（title/url/snippet）→ **smart_truncate 头尾保留截断**（前 70% + 后 30%，中间以"[中间内容已截断]"标记衔接；web_search 的 JSON 截断改为"丢末尾条目保结构"）→ 截断附注。
- **作用与目标**：第一道防线——4 万字网页进门只剩 4 千字符；**在进入上下文之前裁剪，比进去之后再删便宜得多**（每轮全量重发）。
- **注意**：⚠P48-b 头尾保留的直接动因——定价表/页脚信息常在页面中后部，纯头部截断曾把 DeepSeek 定价表整段切掉（q01 supported 0.3 的主因）；字段取舍四标准不变；最终以评测与 trace 验证。

### 小节点 5.2 历史压缩（compact_messages）
- **做什么**：旧历史 → 一次 LLM 摘要（强制保留四类信息：目标/发现/待办/约束）→ 与最近 N 条原文重建上下文。
- **怎么做**：三步——**规范化**（pydantic 转 dict）→ **安全切分**（切分点向后滑动避开 tool 孤儿）→ **摘要重建**（只注入 user 摘要消息，不造 assistant）。返回三元组（messages, summary, **stats**）——stats 含 old/kept/前后长度/**summarizer_input（交给摘要模型的旧历史全文）/summarizer_output（摘要全文）**，供 GUI 下钻。
- **用量全额计入预算**：压缩自身是一次 LLM 调用，其产生的 `cstats["usage"]` **必须 100% 通过 `budget.add` 计入 Agent 运行总预算**，彻底消除"压缩偷跑 token"导致的预算穿透黑洞。
- **作用与目标**：把"滚雪球"变"恒温器"：实测 26 步任务 13 次压缩，token 按在几千级。
- **注意**：摘要有损——事实由笔记兜底（大节点 6/3.6）。

### 小节点 5.3 触发器（循环内检查）
- **做什么**：每轮调用后看真实 `usage.prompt_tokens`，超过 `MAX_CONTEXT_TOKENS` 就压缩。
- **作用与目标**：用真实计量值（而非估算）决定"何时压缩"——让上下文工程从拍脑袋变成有数据触发的自动行为；成本曲线因此可控。
- **注意**：压缩发生在调用之后——单轮工具结果本身超胖时峰值仍会冒头，必须配合进门裁剪（5.1）与单页萃取（5.4）。

### 小节点 5.4 单页 Map 即时萃取（extract_page_facts / Map-Reduce，common/context.py）
- **做什么**：长网页（$\ge 800$ 字符）在抓取落地后、写入上下文之前，调用专职 Prompt 执行单页即时事实萃取（Map 阶段），只保留与当前研究目标（goal）紧密相关的核心事实、数据与结论。
- **怎么做**：
  - 在 `tools.dispatch` 分发中，若文本长度达到 `PAGE_EXTRACT_THRESHOLD`（默认 800 字符），自动调用 `extract_page_facts(client, raw_text, goal)`。
  - 模型以清晰的 Bullet points 提取事实清单，并在文本顶部标注 `[已提取核心事实 · 原长 X 字符 -> Y 字符 · 压缩率 Z%]`。
  - 萃取调用所产生的 token 用量通过 `on_extract_usage=self.budget.add` 回调全额记入总预算。
  - 若萃取失败或未提供 extract_client，自动平滑降级使用截断原文。
- **作用与目标**：生产级 Map-Reduce 架构。长网页原文字数多、信息密度稀疏（含大量修饰语、网站导航、冗余叙述），即时萃取单页通常可实现 **80%+ 压缩率**（4000 字符压至 400-600 字符），极大减缓长任务上下文膨胀速度，同时将噪音过滤在主历史之外。
- **双层防线协同**：与 5.2（历史全局压缩）形成"单页局部提炼（Map）+ 历史阶段摘要（Reduce）"的双层上下文护城河，长程任务兼具轻装与事实保真。
- **注意**：短网页（$< 800$ 字）直通不萃取以节省调用；萃取 token 必须进账；Web 前端同步展示 `📄 萃取提纯` 徽章。

---

## 大节点 6：记忆系统（memory.py）

- **做什么**：把"关于用户本人的稳定偏好"跨会话持久化，新会话经**相关性门禁过滤**后按需注入 system。
- **目标**：agent 越用越懂你。与笔记的边界是生命线：**笔记=任务内研究发现（用完即弃），记忆=用户偏好（长期有效）**——两者混装是头号设计错误；同时**杜绝记忆负迁移**（无关历史偏好污染当前课题）。
- **技术**：JSON 文件 + LLM 提取（JSON mode，提示词硬性划界）+ 规则去重合并 + 容量上限 20 条 + **双轨门禁（全局通用偏好 100% 注入 + 领域偏好意图/实体相关性门禁）**。
- **作用（产品价值）**：个性化留存且防带偏。"上次说过用中文+表格对比"全域生效；但"上次关注豆包会员定价"绝不会带偏当前的"飞书打通豆包工作模式"纯架构课题。
- **与下一节点衔接**：注入发生在 agent 启动组装 system 之前，抛送 `memory_inject` 事件（含 count/total/filtered）；提取发生在会话结束（走大节点 1 的 call_llm，事件带 transcript 全量进 GUI）。
- **注意事项**：⚠P24 提取必须有回显；错误偏好需用户可编辑接口（生产）；⚠P45 记忆负迁移：无关领域偏好强行注入会诱导模型发起多步偏航检索并烧穿预算。

### 小节点 6.1 存储与精准注入（MemoryStore / format_for_prompt / 相关性门禁）
- **做什么**：读写 memory.json（facts 列表，每条 text+ts）；会话启动按当前课题执行**相关性门禁（Relevance Gating）**：
  1. **通用偏好（Global Preferences）**：如语言（简体中文）、呈现形式（表格对比呈现）、研究输出规范（严禁编造/保留来源与URL）100% 注入，跨会话全域生效。
  2. **领域偏好（Domain Preferences）**：如特定竞品会员定价比价、微信小程序合规等，必须同时满足：
     - **意图门禁**：若偏好含定价/计费关键字，用户课题必须包含定价/费用意图，非定价课题坚决不召回；若偏好含认证/合规关键字，课题须有对应合规意图；
     - **实体与 Bi-gram 实体交集**：通过关键词与 2-gram 字符级滑动窗口匹配实体交集。
  3. **空记忆与无匹配处理**：空记忆或全部不相关时返回空串，不占 token。
  4. **可观测指标**：注入触发 `memory_inject` 事件，透传 `{count, total, filtered}` 供前端展示过滤与注入情况。
- **作用与目标**：让 agent"越用越懂你"而**不**"被历史带偏"——门禁是记忆系统的安全阀：通用偏好全量生效保证一致性，领域偏好按需召回防止负迁移。

### 小节点 6.2 偏好提取（extract_preferences）
- **做什么**：会话结束，一次 LLM 调用从 transcript 抽偏好。
- **作用与目标**：把对话流蒸馏成可复用的偏好——agent 从"一次性工具"升级为"长期助手"的关键一步；划界不严会把调研内容当偏好，污染之后所有任务。
- **注意**：提示词硬性划界——只收用户本人的稳定偏好；**调研内容、临时要求严禁混入**；GUI 的 memory_extract 事件带 transcript 全量（输入）与偏好（输出）。

### 小节点 6.3 合并（merge）
- **做什么**：**LLM 语义合并为主、规则判重兜底**（⚠P48 升级）——把新偏好与现有记忆一起交给 LLM 做同义合并 / 冲突以新为准 / 格式统一（JSON mode 输出）；规则层只挡精确/包含重复。合并时**保留原条目时间戳**（旧记忆不因改写而"变新"）；上限 20 条丢最旧。
- **作用与目标**：让记忆库"自我清洁"——同义不重复、改主意以新为准、总量有上限；没有这一步，记忆只会膨胀成噪音，个性化反而变成干扰。
- **注意**：子串判重挡不住"近重复"（"结论用表格" vs "结论偏好用表格形式"）——语义合并实测近重复不涨、真新偏好正常收录；合并调用走大节点 1 的 call_llm。

---

## 大节点 7：安全件（guardrails.py）

- **做什么**：危险分级与人工确认、预算记账与三水位收尾。
- **目标**：回答"agent 出错时，谁来兜底、代价多小"。三件分工——**预算兜"跑飞了"，HITL 兜"做不该做的事"，记忆兜"理解错偏好"**。
- **技术**：真实计费值记账；终端 y/n（生产换审批系统）；分级注册表。
- **作用（产品价值）**：demo 变产品的分界线。
- **与下一节点衔接**：HITL/预算嵌在大节点 4 循环里；MCP 外部工具自动进分级表（大节点 13.4）。
- **注意事项**：⚠P30 只拦危险动作（少而准，防批准疲劳）；⚠P31 拒绝回执话术决定后续行为；⚠P44 软性收尾挡不住超支。

### 小节点 7.1 危险分级（DANGEROUS_TOOLS）
- **做什么**：`{工具名: 危险原因}` 注册表；判断标准五类——不可逆/对外发送/花钱/改权限/低置信。
- **衔接**：MCP 外部工具接入时自动扫描（write/edit/move/create）升级进表（大节点 13.4）。
- **作用与目标**：划定"哪些动作必须人点头"——不可逆/对外/花钱/改权限的操作，模型不能自己拍板；它是 HITL 闸门（7.2）的触发依据。

### 小节点 7.2 人工确认（ask_human）
- **做什么**：sensitive 工具执行前，终端展示工具名/参数/危险原因，y/n 决定。
- **作用与目标**：给危险动作装物理闸门——模型说"要发"没用，用户点了"批准"才算；拒绝回执让模型体面改道而不是反复尝试。
- **注意**：⚠P31 **拒绝回执话术决定后续行为**——固定为"不要重复尝试同一动作，改为直接输出内容"，实测模型完全服从。生产换 IM/工单异步审批，"暂停-询问-回填"骨架不变（Web 版见大节点 15.3）。

### 小节点 7.3 预算（Budget）+ 档位
- **做什么**：累计 usage.total_tokens（真值记账）；三水位——`near_limit(0.8)` 软提醒、`near_limit(0.95)` **硬收尾（拔工具箱：最后一次调用不传 tools，模型物理上无法再发起工具调用）**、`exhausted` 强停。
- **档位**：BUDGET_TIERS（quick 2万 / standard 5万 / deep 18万）——**按任务类型分级，而非全局一刀切**。依据：60k 一刀切会腰斩合法深度任务（实测 85-108k），对简单查询又虚高；分档 = 把"成本 vs 完整度"权衡交还用户（Anthropic effort scaling 思路）。
- **作用与目标**：给 agent 装"油表 + 限速器"——token 真值记账、三水位递进收紧；保证任何任务都有明确的成本上限，且到线必停（不靠模型自觉）。
- **注意**：⚠P44 软提醒实测三次全被无视——硬收尾才是保证（**物理约束 > 行为请求**，本项目验证两次）；收尾调用本身耗 token（实测 2967/3000、4563/3000），95% 触发线就是为它留的余量。

### 小节点 7.4 优雅收尾（WRAP_UP_MSG + 笔记兜底）
- **做什么**：收尾指令三要素——汇报已有进展 + 列未完成项 + 说明续跑方式；**若强制结题调用仍失败，读 NOTES.md 原文作为最终报告输出**（信息不能跟着失败的调用一起丢）。
- **作用与目标**：实测两级都有效——正常时输出规范《阶段性结题报告》；异常时输出笔记原文兜底。

---

## 大节点 8：多智能体（orchestrator.py + sub_agent.py）

- **做什么**：主 agent 拆题、派工，多个子 agent 并行调研，各回传浓缩结论，主 agent 汇总。
- **目标**：并行省时（实测 2.5x）+ 上下文隔离（子 agent 读几万 token 只回传 ≤700 字，主 agent 永远轻装）。
- **技术**：LLM 拆题（JSON mode）+ ThreadPoolExecutor 并行 + thread-local 笔记 + 错峰启动 + 死线护栏。
- **作用（产品价值）**：单 agent 的"上下文肥胖"用**组织分工**解决；也是"100 个工具怎么编排"的终极答案（分层：调度员只拿部门级工具）。
- **与下一节点衔接**：多智能体产出与单 agent 同台进评测打擂台（大节点 10/模式对比）；每个子 agent 内部就是一个大节点 4 的循环。
- **注意事项**：⚠P13 子 agent 同样需要死线护栏（首跑全员罢工）；⚠P26 thread-local；⚠P27 任务书自包含不重叠；⚠P28 能不拆就不拆；⚠P29 诚实验收逐级传导（实测子 agent 全军覆没时主 agent 输出"本次调研失败，请勿用于决策"）。

### 小节点 8.1 拆题（decompose）
- **做什么**：一次 LLM 调用拆成 1~4 份**自包含**任务书（目标+查证点+边界），附拆分策略。- **作用与目标**：多智能体的"分蛋糕"环节——任务书切得自包含、边界清晰，worker 才能并行不串台；拆分质量直接决定这个模式的收敛速度与上限。
- **注意**：规划器会贪多（完美主义清单导致永远做不完）——数量限制必须写进 prompt；拆分质量决定收敛性。

### 小节点 8.2 并行派工（dispatch）
- **做什么**：ThreadPoolExecutor 并行跑 worker；**错峰启动**（每个延迟 5 秒缓解搜索限流）；统计墙钟 vs 串行（实测 2.5x）。
- **作用与目标**：用并行换时间——相互独立的子任务同时跑，墙钟时间约等于最慢的 worker（实测 2.5x 提速）；错峰启动是对外部搜索 API 限流的工程让步。
- **注意**：worker 异常捕获成"失败报告"，不让线程崩掉拖垮全局。

### 小节点 8.3 子 agent（run_sub_agent）
- **做什么**：领任务书 → 独立循环调研（搜索/抓取/笔记/压缩/死线一应俱全）→ 输出**浓缩结论**（≤300 字要点+来源，"未查到"如实标注）。
- **生产级增强**：
  - **独立会话工具缓存**：每个 worker 实例化专属 `SessionToolCache`，搜索精准/模糊去重与网页防重拉取在 worker 内闭环生效，互不干扰。
  - **单页 Map 即时萃取**：长网页抓取进门即由 LLM 提炼核心事实，压缩比 80%+，极大减轻子上下文负担。
  - **压缩用量闭环记账**：压缩与萃取产生的 token usage 100% 计入子 Agent 局部 budget。
  - **输出 DSML 净化**：向主 agent 返回结论前同样执行 DSML 标签过滤与笔记兜底。
- **作用与目标**：每个 worker 是一个"完整但缩小"的 agent——独立循环 + 全套护栏；输出强制浓缩（≤300 字），保证主 agent 的上下文不被撑爆。
- **注意**：无拆题权（防套娃）；独立笔记（thread-local）；死线护栏出厂标配。

### 小节点 8.4 汇总（synthesize）
- **做什么**：主 agent 一次调用综合各调查员结论 → 最终对比报告。
- **作用与目标**：把多份调查员结论合成一份报告，并负责**诚实验收**——子级"未查到"如实传导，严禁主 agent 脑补圆场；责任链的最后一环。
- **注意**：system 必须写"调查员标'未查到'的项如实说明，不许脑补"——实测子 agent 全军覆没时，主 agent 输出"本次调研失败，请勿用于决策"（诚实验收沿责任链传导）。

---

## 大节点 9：结构化输出与规划（research_agent_v3.py）

- **做什么**：三段式交付——Plan（JSON 研究大纲）→ Execute（按大纲调研）→ Report（带 [n](url) 引用的报告）。
- **目标**：从"自由发挥"到"按合同交付"：输出可解析、过程不漏项、结论可核验。
- **技术**：`response_format=json_object`（JSON mode 强制合法 JSON）+ 复用循环 + 引用格式约束。
- **作用（产品价值）**：**混血模式**——规划是 workflow 思维（固定骨架），调研是 agent 思维（自由执行）；结构保证覆盖度，放权保证适应性。
- **与下一节点衔接**：答案+证据进大节点 10 评测；收敛护栏（4.4）在此阶段发明。
- **注意事项**：⚠P13 收敛护栏；规划器贪多需限制子问题数量（≤3、"10 步内可完成"）。

### 小节点 9.1 规划（plan）／9.2 执行（research）／9.3 报告（Report）
- 9.1：JSON mode 产出大纲；9.2：复用循环（工具+笔记+压缩+死线），逐步打印决策过程；9.3：收尾前强制 note_read，按笔记输出带引用报告。
- **作用与目标**：v3 是"结构化输出 + 三段式职责分离"的参照实现（规划/执行/报告各司其职）——现在的生产内核就是从它演化来的，留作对照。
- **注意**：过程可视性是调试前提（Stage 5 靠它发现不收敛）。

---

## 大节点 10：模式与选型（patterns.py + ab_experiment.py）

- **做什么**：亲手实现 workflow 固定管道与 Evaluator-Optimizer 反思模式，并与自由 agent 三方同题对比（同一评测集 + 同一 judge）。
- **目标**：回答产品决策问题——**什么时候用 workflow，什么时候放权给 agent**。
- **技术**：固定四段管道（模板拼词→逐个执行→一次成文）；草稿→评审（JSON pass/issues）→修订循环（≤2 轮可提前退出）；三方实验框架。
- **作用（产品价值）**：实测数据——A 纯 workflow 0.57~0.82 波动、2.1k tok、46s；B 反思 0.55~0.61、21k；C 自由 agent 0.68~0.78 稳、28k+。**质量差距小→选便宜的；深度任务 C 值得 13 倍成本。**
- **与下一节点衔接**：选型结论进评测基线；模式思想进多智能体（大节点 8/13）。
- **注意事项**：⚠P39 **反思修订者必须能对意见"采取行动"**（纯文字修订≈化妆品，实测 B 反而低于 A）；**单题噪声大（同题分差 0.2+），结论只看多题均值**。

### 小节点 10.1 workflow 管道（run_workflow）
- **做什么**：四段固定管道——模板拼搜索词（不经模型）→ 逐个执行 → 全部证据一次成文 → 结束。无循环无决策。
- **作用与目标**：agent 的经济学参照物；弱点实测：证据质量决定天花板（搜索词模板拼的，命中差则覆盖塌方）——**workflow 的质量上限 = 管道设计者的预见力**。

### 小节点 10.2 反思模式（run_reflective，修订者带工具）
- **做什么**：workflow 草稿 → 评审循环（≤2 轮，pass 提前退出）：
  ① **评审员只核对**（问题/证据/草稿三方一致性；JSON 输出 `{pass, issues}`，意见必须具体可修）；
  ② **修订策划**：LLM 逐条判断哪些意见需要"新的外部证据"才能修（缺数据/缺出处/覆盖缺口），生成定向搜索词（每轮最多 2 次）；
  ③ **补查执行**：真实 `web_search`（Tavily 优先），结果**并入证据池**——下一轮评审员可见；
  ④ **修订者**：拿意见 + 新证据改稿，新事实同样加 [n](url) 引用。- **作用与目标**：演示 evaluator-optimizer 的完整闭环，并验证一句话——**批评只有能被行动解决才有价值**；核对（评审员）与行动（修订者补查）分工后，反思循环从空转变有效。
- **为什么升级**：⚠首跑翻车（B≤A）——旧版修订者纯文字改稿补不了证据，只能删句/脑补/绕行，反思循环空转。升级后批评落在事实层时能被行动解决。
- **实测**：第 1 轮评审 4 条意见（64K/128K 信源矛盾未解释）→ 策划出 2 条定向补查 → 带新证据改写 → 第 2 轮 pass=true。另修存量 bug：`call_llm` 返回消息对象，正文必须取 `.content`（此前评审员评审的是对象的 repr 乱码，pass 判定失真）。
- **注意**：**判断权在模型**（哪些意见要补查、查什么），**执行权在代码**（每轮补查次数上限、统一走 web_search、证据池合并）；策划 JSON 解析失败自动降级为纯改写；本节点是实验田（stages/06，CLI 直跑），未接入工作台主链路——工作台执行流展示的是生产内核（大节点 4）的节点。

### 小节点 10.3 三方对比实验（ab_experiment.py）
- **做什么**：同题集 × 三实现 × 同 judge → 质量/tokens/用时对比表。
- **作用与目标**：把"哪个模式好"从感觉变成数据——同题集、同 judge、只换实现，胜负有据；它也是评测系统（大节点 11）的第一位真实用户。
- **注意**：⚠P34 计量必须走统一入口（实测绕过 call_llm 导致 A/B token 计量为 0）。

---

## 大节点 11：评测系统（evals/）——给 agent 装"考试体系"

- **做什么**：12 道真实研究题（6 factual + 4 comparison + 2 open）+ LLM-as-judge 四维评分器 + 一键运行器；每轮评测产出落 `evals/results/` 的成绩单（总分、维度分、逐题问题清单、答案全文）。
- **目标**：让每次改动都有分数可对比——**没有 eval 的迭代 = 没有验收标准的 PRD**。
- **作用（产品价值）**：把"感觉变好了"变成可追溯的分数曲线（实测 0.57 → 0.66 → 持续优化中）；回归验收、质量定位、选型决策三件事全靠它——它是整个迭代飞轮的启动齿轮。
- **技术**：题库 JSON（每题带 key_points）；judge 单模型四维 rubric（有据/有源/覆盖/简洁）JSON mode 输出；运行器串行跑题聚合报告。
- **与下一节点衔接**：分数进可观测面板（大节点 12）；groundedness 依赖的证据来自运行时截留。

### 小节点 11.0-a 为什么评测？——三个没有评测就踩的坑

1. **进步无法证明**："我觉得加了萃取之后更好了"——感觉不构成验收。有评测，才说得出"基线 0.68 → 0.76"这种话。
2. **退化无法察觉**：改提示词救活了 q01，可能悄悄弄坏 q05——隐形回归没有评测就是隐形的。改前改后各跑一遍，退化当场现形。
3. **取舍无法决策**：workflow、agent、反思模式到底哪个好？同题集、同 judge、只换实现，跑分说话——把 PM 最核心的"选型判断力"从感觉变成数据（心法 1）。

一句话：**没有 eval 的迭代，等于没有验收标准的 PRD**——你不会给没有验收标准的需求写功能，也不该给没有评测的 agent 改代码。

### 小节点 11.0-b 什么时候评测？——五个触发时机

| 时机 | 跑法 | 目的 |
|---|---|---|
| 任何改动前后（回归） | 改前全量基线，改后同规格重跑 | 验证修复有效、无隐形退化 |
| 新模式/新架构选型 | A/B：同题集只换实现 | 用数据选型（心法 1） |
| 修 bug 之后 | 只跑相关题（`--id q02 q07`） | 确认修好且不伤别的题 |
| 定期健康检查 | 全量 | 防外部环境漂移（搜索源质量变化、模型供应商静默更新） |
| 行为/分数异常时 | 指定题复跑 | 区分"真退化 / 偶然噪声 / 系统故障" |

成本参考：`--n 3` 快跑约 10 分钟，全量 12 题约 30 分钟——一次回归验证的成本远低于"带着隐形退化继续开发"的代价。

### 小节点 11.0-c 评测的目标与价值——四维各自防什么

judge 按四维 rubric 打分（各 0~1，总分 = 均值），每一维都在防一种典型失败：

| 维度 | 防什么失败 | 0 分触发条件 | 当前基线 |
|---|---|---|---|
| supported 有据 | **编造**：结论在证据里找不到支撑 | 编造/证据不足 = 0 | 0.54（最大短板 → 下一步主攻证据覆盖率） |
| sourced 有源 | **无出处**：事实不挂引用 | 没有引用 = 0 | 0.83（强项：引用规范已练成） |
| complete 覆盖 | **跑题**：漏答用户的核心要求 | 缺一半以上 = 0 | 0.72 |
| concise 简洁 | **注水**：复述证据原文、空话套话 | 严重冗长 < 0.5 | 0.54 |

**灵魂设计 groundedness**：judge **不上网**。它拿到的不是"世界真相"，而是 agent 调研时工具返回的**原始证据**——所以它评的是"结论是否忠于你自己查到的证据"，专抓编造（首跑就抓到 Team 档 $1590 无证据的虚构价格）。一个研究 agent 的本职是：查到什么说什么，查不到就承认——supported 维度就是这条职业操守的考官。

### 小节点 11.0-d 评测标准如何定义——业界通用体系 + 本项目的锚点

**一、标准 agent 评测的通用指标体系（四层，业界大体标准）**

| 层 | 指标 | 说明 |
|---|---|---|
| 任务层（结果对不对） | 任务成功率 / 端到端正确率 | 最终交付物是否达成目标；有标准答案时做比对 |
| 过程层（行为对不对） | 工具调用正确性、步数效率、指令遵循 | 选对工具了吗、参数合法吗、系统提示词的硬性规则守了没 |
| 内容层（质量高不高） | 有据性 groundedness、相关性、覆盖率、引用正确性 | 结论被证据支撑吗、切题吗、问的点都答了吗、引用真实且指向正确吗 |
| 工程层（划算吗、稳吗） | 延迟、成本（token/步数）、稳定性方差、鲁棒性、安全性 | 多次运行结果抖不抖、错误能不能恢复、危险操作拦不拦得住 |

**二、四类主流评测方法（各有分工，通常组合使用）**

1. **确定性断言**（代码校验）：格式/schema/必含字段/关键词命中——快、零噪声，管"底线"；
2. **要点覆盖比对**：key_points 命中率——管"任务完成度"；
3. **LLM-as-judge**（rubric 打分）：质量类主观维度——管"好坏"，需要校准；
4. **人工评估**：golden set 抽查复核——最准最贵，用于**校准 judge**。

本项目的组合：sourced/cited 用断言思路（有引用编号与否代码可判）；supported/complete/concise 用 judge；judge 的低分题定期人工抽查校准。

**三、本项目的标准定义与锚点（rubric calibration）**

| 维度 | 定义（标准表述） | 0 分锚点 | 满分锚点 |
|---|---|---|---|
| supported | 关键结论均能在原始证据中找到支撑 | 编造/证据不足 | 每个事实有对应证据 |
| sourced | 每个事实带 [n](url) 引用 | 无引用 | 全文规范引用+参考小节 |
| complete | 问题及大纲要求的信息点全部覆盖 | 缺一半以上 | 全覆盖且有查证要点 |
| concise | 无复述证据原文、无空话 | 严重冗长 | 结构清晰、密度高 |

配套阈值与红线（PM 定验收标准的方式）：
- **合格线**：如 supported ≥ 0.7 才算及格；
- **回归红线**：任何改动不得让任一维度低于基线 - 0.05；
- **噪声治理**：单题分差 0.2+ 属正常噪声，结论只看多题均值；judge 解析失败记 0 但必须标注（区分"真差"与"故障"）。

**四、定义标准的原则（PM 视角）**

1. **标准从需求来**：题库的 key_points 就是 PRD 的验收条款——评什么，agent 才练什么；
2. **标准与核心风险对齐**：本项目最大风险是编造 → supported 是灵魂维度；换一个场景（如代码生成 agent）标准体系会完全不同（编译通过率/测试通过率）；
3. **可复现**：同输入、同标准、同判——标准要写死在 rubric 里，不能凭 judge 心情；
4. **标准也要迭代**：agent 升级后旧标准会失效（如新增会话能力就该加"多轮一致性"维度）——评测体系是活的。

### 小节点 11.0-e 分数怎么来的？——六步流水线

```
① 取题：问题 + key_points（必须查到的要点）
② 考试：生产内核真跑一遍完整研究（同工作台，标准档预算）
③ 截留：过程中把每次搜索/抓网页的返回原文存成"证据池"
④ 阅卷：judge 拿（问题 + 大纲 + 证据前12条 + 报告）对照核对，JSON mode
   输出 {supported, sourced, complete, concise, total, issues[]}
⑤ 聚合：12 题均分 + 维度均分 + 逐题问题清单
⑥ 落盘：evals/results/eval_日期_时间.md 成绩单（含答案全文供复核）
```

关键细节：③证据截留是灵魂（没有它，judge 只能评文风）；④解析失败 = 四维全 0（q10 得 0 分的真相——不是 agent 太差，是 judge 那次自己坏了）；单题噪声大（分差 0.2+），**结论只看多题均值**。

### 小节点 11.0-f 评测之后：谁来解决问题、怎么解决

**分工：机器负责发现与打分，人负责终审归因与修复。** 评测系统自己不修任何东西——它只把"哪里差、差多少、依据是什么"摆到你面前。

**第一步：把低分题分成三类（性质完全不同，处理方式不同）**

| 低分类型 | 特征 | 处理 |
|---|---|---|
| 系统故障 | judge 解析失败（q10）、答案含 DSML 残留（q12） | 修评测器容错 / 修硬收尾路径——这不是 agent 能力问题 |
| 偶然噪声 | 单题突然低分，重跑就恢复 | 重跑取均值，**不动代码** |
| 真实能力缺口 | 多次评测稳定低分，issues 指向同类问题 | 进入归因流程 ↓ |

**第二步：归因——从"哪里差"到"病在哪一环"**

评测只给现象（supported=0.54），不给病因。归因靠读 trace（执行流里逐颗看：检索到了吗？→ 抓到正文了吗？→ 萃取留住了吗？→ 压缩丢了吗？→ 模型引用了吗？），定位病灶在**哪一层**。

**第三步：修复——任何一层都可能是病灶，下刀要小**

历史修复账本（评测发现 → 修复位置 → 层）：

| 评测发现 | 修复 | 层 |
|---|---|---|
| supported 低：页尾定价数据丢失 | `smart_truncate` 头尾保留截断 | 工具截断策略 |
| 编造数字（$1590 无证据） | 提示词数字纪律：无证据一律标"未验证" | 提示词 |
| 引用不规范 | 引用格式硬性要求 [n](url) | 提示词 |
| 不收敛、反复重搜 | 熔断器 + 充分度门禁 + 阶梯自检 | 护栏 |
| 重复检索浪费 | SessionToolCache | 缓存 |

**归因口诀：评测出分数，trace 找病灶，修复下刀要小，回归验证明效。**

（历史基线：这套循环跑下来，总分从 0.57 → 0.66 → 目标 0.76+；supported 从 0.54 起步仍是当前主攻方向。）

### 小节点 11.1 题库（questions.json）／11.2 评分器（judge.py）／11.3 运行器（run_eval.py）
- 11.1：12 题，小而具体（5-12 步可完成），从真实需求来，每题带 key_points（必须查到的要点，供 judge 核覆盖）。
- 11.2：judge 不上网查证，拿 agent 调研时的原始证据核对"结论是否被支撑"——首跑抓到编造价格（Team 档 $1590 无证据）。解析失败自动降级为全 0 + 标注（q10 教训：judge 自身也要容错）。
- 11.3：`--n 3` 快跑 / `--id q02 q07` 指定题 / 全量基线；改动前后各跑一遍对比——这是回归测试的完整闭环。

---
- **作用与目标**：给 agent 建"考试体系"——题库是考卷，judge 是只对照原始证据的阅卷老师，运行器是教务处；三者合起来，任何改动都有分数可查、可回归。

## 大节点 12：可观测性（observability.py + dashboard.py + classify_failures.py）

- **做什么**：所有运行统一落结构化 JSONL；LLM 归类失败模式；生成静态观测面板。
- **目标**：三问随时可答——成功率多少？token 花在哪？失败归哪几类？
- **技术**：JSONL 追加写；LLM 归类（固定类目 + 教边界："审批拒绝是正常拦截不算故障"）；纯 HTML 面板无依赖。
- **作用（产品价值）**：agent 上线后 PM 的日常 = 看数据、归类失败、驱动改进（相当于增长 PM 看漏斗）。
- **与下一节点衔接**：RunLog 是全系统唯一运行数据源（含 GUI 运行）；诊断探针（12.4）是它的精查形态。
- **注意事项**：⚠P34 统一入口；坏行跳过不吞错；面板合格标准三问。

### 小节点 12.1 运行日志（log_run / load_runs）／12.2 失败归类（classify_failures.py）／12.3 观测面板（dashboard.py）
- 12.1：每次运行追加 JSON（run_id/时间/实现/问题/步数/工具序列/tokens/预算/stop_reason/errors/extra）。
- 12.2：LLM 把非正常收尾归入固定类目，输出分布表+逐条依据。
- 12.3：四指标卡 + 失败分布 + 运行历史表，静态 HTML。
- **作用与目标**：agent 的"体检报告 + 病历本"——每次运行留痕、失败自动归类、面板直答三问（成功率/花费/失败类型）；上线后运营的日常抓手。

### 小节点 12.4 结构诊断探针（DEBUG_DUMP）
- **做什么**：强制结题等关键路径失败时，自动 dump 消息结构（每条的角色/声明的调用id/结果id/内容头）到 `logs/debug_structure_*.json`。
- **作用与目标**：配对类 400 的定位神器——不猜，直接看哪条消息未配对。

---

## 大节点 13：MCP 接入（mcp_client.py）

- **做什么**：agent 作为 MCP Host 接入外部 MCP server，动态发现并调用外部工具。
- **目标**：工具从"自己写死"升级为"生态动态接入"——核心代码零改动（MCP 只标准化工具的发现/描述/调用，不碰循环与上下文）。
- **技术**：官方 mcp SDK；后台线程 asyncio 会话常驻 + `run_coroutine_threadsafe` 同步接口；inputSchema→OpenAI tools 转换（前缀防冲突）；**共享单例 + 生命周期管理**。
- **作用（产品价值）**：接入新工具的成本 = 改一行配置（MCP_SERVERS JSON）。
- **与下一节点衔接**：MCP 工具合并进 tools 参数后与本地工具无差别使用；写类自动进危险分级（大节点 7.1）。
- **注意事项**：⚠P4 蛇形命名（input_schema）；anyio TaskGroup 包裹异常必须打 traceback；**外部工具默认不可信**——写类自动升级危险级走 HITL。

### 小节点 13.1 会话管理与生命周期（start / _main / close / 共享单例）
- **做什么**：后台线程事件循环维持 stdio 会话；`_stop_evt` 停止信号 → stdio 上下文退出 → 子进程终止；join 超时后按参数特征 pkill 兜底；`get_shared_client` 同 server 全项目只连一次；atexit 兜底。
- **作用与目标**：⚠P45 修复"每次运行 spawn 新进程"的泄漏（实测跑 10 次 = 10 个僵尸进程）。
- **注意**：close 后线程必须退出、子进程 0 残留（已实测）。

### 小节点 13.2 工具发现与转换（openai_tools）
- **做什么**：`list_tools()` 结果 → 每个工具转 `{"type":"function","function":{name(加前缀),description,parameters}}`。
- **作用与目标**：**MCP 的 inputSchema 就是 JSON Schema**——和你手写的菜单卡同源，"换信封"即可。

### 小节点 13.3 调用转发（call_tool / owns）
- **做什么**：dispatch 遇带前缀工具名 → 剥前缀 → `session.call_tool` → 拼接文本内容回填。
- **作用与目标**：让外部工具对模型"零感知"——剥前缀后与本地工具走同一套回填协议；生态接入的最后一公里。

### 小节点 13.4 安全联动（危险分级自动延伸）
- **做什么**：接入时扫描工具名，含 write/edit/move/create 自动注册进 DANGEROUS_TOOLS。
- **作用与目标**：**接入生态的同时，安全件自动延伸到生态**——外部工具默认不可信。

### 小节点 13.5 配置（三级解析）
- **做什么**：`MCP_SERVERS` 环境变量（JSON 数组：name/command/args）> `MCP_FS=1` 旧开关（默认 filesystem 指向项目目录）> 不启用。

---
- **作用与目标**：把"接不接 MCP、接哪个 server"降为一行环境变量——接入新工具生态的成本趋近于零。

## 大节点 14：辅助设施（学习与调试工具）
- **作用**：学习与调试的基建——把"发请求""看全程""留数据"变成随手可用的工具。
- **目标**：学习的效率杠杆——别人调 agent 靠 println，你靠实验室、捕获器和数据资产。

### 小节点 14.1 交互实验室（playground/）
- **做什么**：FastAPI 透明代理 + 表单页：任意角色/工具/参数构造请求，回显实际请求体 + 原始响应（含思考模式开关、格式化输出三面板）。
- **作用与目标**：把"发请求"变成交互实验；报错原文也是教材（developer 角色 400、max_tokens 截断都在这里亲眼见过）。
- **注意**：key 走后端 .env 不经浏览器；透明原则——不做任何解析纠错。

### 小节点 14.2 完整输入输出捕获（capture_full_io.py）
- **做什么**：挂 CallRecorder 跑一次 agent，逐次导出完整请求/响应 Markdown，文末附自动校验四项（快照递增/id 配对/finish_reason/token 复核）。
- **作用与目标**：学习材料生成器 + 深度调试器。
- **注意**：⚠P36 深拷贝快照（引用污染教训的产物）。

### 小节点 14.3 数据资产（traces/ notes/ logs/ observatory/）
- **做什么**：traces 存每次运行完整消息流；notes 存学习复盘与输入输出存档；logs 存 RunLog 与失败报告；observatory 存面板。- **作用与目标**：给学习过程留"可回放的证据"——每个结论都能翻回当时的原始数据；也是复盘笔记与对外分享的素材库。
- **注意**：traces 内容不进 git（.gitignore），结构与学习价值在内部分享时展示。

---

## 大节点 15：研究工作台 GUI（webapp/）

- **做什么**：把 agent 从命令行搬进浏览器——研究工作台单页：输入问题（可选档位）→ **SSE 实时层级执行流**（编号颗粒 + 可下钻详单 + 计时）→ HITL 审批横幅 / ⏹ 中止 → MD 渲染的最终报告 → 左栏历史点击进入**详情回放**。
- **目标**：三件产品化——过程可视性（透明原则从开发者特权变用户权利）、控制权（批准/拒绝/中止/档位变按钮）、资产入口（历史/报告/记忆可回看）。
- **技术**：FastAPI + **SSE** + 原生 HTML/JS（延续不上框架路线）；核心是**事件化改造**——`ResearchAgent` 注入 Web 实现（emit→SSE 队列、approver→网页按钮、stop_check→中止按钮），**一个内核两种脸，决策逻辑零改动（评测回归验证）**。
- **作用（产品价值）**：agent 从脚本变产品的临门一脚。
- **与下一节点衔接**：审批端点是 7.2 的 Web 化；历史回放数据源是 12 的 RunLog + 事件存档。
- **注意事项**：⚠P36 类前端三连事故（tier 绑定丢失/msBadge 误删/loadHistory 漏调用）——**小步提交 + 浏览器实测，缺一不可**；审批是阻塞设计（线程等 Event 最多 600s），生产换任务队列+轮询。

### 小节点 15.1 事件化内核注入（WebResearchAgent）
- **做什么**：继承 ResearchAgent，注入 Web 三件——emit 推进线程安全事件队列（并落盘事件存档）、approver 阻塞等网页按钮（threading.Event + POST 回填）、stop_check 查中止按钮。
- **作用与目标**：内核"换脸"的插座——同一份决策逻辑，接 CLI 出终端流，接 Web 出 SSE 流；事件化是整个 GUI 的数据地基。
- **注意**：线程对象必须回填给 agent（SSE 生成器靠它判断运行是否结束——实测缺失即 AttributeError 断流）。

### 小节点 15.2 SSE 事件流（/api/research/{id}/events）
- **做什么**：生成器轮询事件队列，`data: {...}` 逐条推送；线程结束且事件发完推 done 收流。
- **作用与目标**：把运行中的每个动作实时推进浏览器——用户从"黑盒等待"变成"看着它思考"；也是断线诊断（15.12）与历史回放（15.5）的载体。
- **注意**：事件负载含全量 IO 详单会很大——本地单用户可接受，多用户部署要降采样。

### 小节点 15.3 HITL 审批端点（/approve）
- **做什么**：approval_request 到达 → 前端横幅"批准/拒绝" → POST 回填 → 阻塞的 approver 放行 → 循环继续。
- **作用与目标**：7.2 骨架的 Web 化；拒绝回执话术原样保留。

### 小节点 15.4 中止端点（/stop）
- **做什么**：点击设置 stop 标志 → 循环下一轮检测 → 注入收尾指令 → 优雅结题。
- **作用与目标**：用户随时能踩刹车，且停得体面。

### 小节点 15.5 事件存档与历史回放（/api/runs/{id}/events + 左栏点击）
- **做什么**：事件 emit 时同步落盘 `logs/events/events_{id}.jsonl`（每行一个 JSON）；回放接口返回 `{events, meta}`（meta 为 RunLog 元信息含 question/stop_reason/tokens/session_id）；前端用与实时相同的统一渲染器重放（审批按钮只读）。**存档头部第一条为 `run_meta` 事件**（run_id/session_id/question，见 15.13），回放时据此找到所属会话。
- **作用与目标**：历史任务可复盘——"这次为什么没查到""压缩发生在哪"逐颗回看；也是"传输层断了 vs 内核死了"的诊断依据（runs.jsonl 有收尾记录 = 传输层问题）。
- **注意**：早于存档功能的旧运行显示提示；回放模式选中条目高亮。

### 小节点 15.6 层级编号执行流（前端）
- **做什么**：按"步"分组成卡片，步内动作自动编号 `步.序`（0.1 JSON大纲规划 → N.1 模型调用 → N.2 工具 → N.末 回填确认），每颗粒带用时、可展开详单；压缩颗粒展开显示目的/方法/统计/摘要全文。
- **大纲与徽章可视化**：
  - **Plan 规划卡片**：在第 0 步专门渲染蓝紫色大纲卡片，展示研究课题、拆分子问题与推荐数据源。
  - **缓存命中微章**：工具执行若命中会话缓存，展示 `⚡ 命中缓存/免请求` 徽章，区分精确秒回与模糊引导。
  - **萃取提纯徽章**：网页正文萃取后展示 `📄 萃取提纯（原长 X字 → Y字 · 压减Z%）` 徽章与指标。
- **怎么做**：后端 `_emit_sub` 自动编号（每步重置）；前端 `<details>` + 编号列表分组渲染。
- **作用与目标**：把线性事件流组织成"步 → 动作"的可读层级——学习时可逐颗对照 realize.md 小节；徽章让关键行为（缓存命中/萃取提纯）一眼可见。
- **注意**：编号与 realize.md 小节一一对应，学习时逐颗对照。

### 小节点 15.7 可下钻详单 + 计时（前端）
- **做什么**：模型调用颗粒展开 = ① 完整输入（messages 全量）② 完整输出（全字段）③ token 明细；工具颗粒展开 = 参数 + 完整输出；每项带 ⏱ 用时。
- **作用与目标**：**屏幕上显示的 = API 实际传输的**——学习工具的合格线。

### 小节点 15.8 MD 报告渲染与前端净化
- **做什么**：final 事件带 answer → 前端先执行 DSML 标签过滤（二次净化剔除 `<｜｜DSML｜｜...>` 假调用代码）→ 转义防注入 → marked 渲染（表格/标题/链接新窗口/代码）→ 无报告时面板整个不显示。
- **作用与目标**：报告是最终交付物，渲染质量决定"产品感"；前端净化 + 降级保证任何情况下不白屏、不漏内容、不执行坏代码。
- **注意**：⚠P46 CSS `.report` 曾残留 display:none 导致"源码有内容页面空白"——显隐只由父面板控制；marked CDN 失败降级纯文本。

### 小节点 15.9 预算档位选择器
- **做什么**：三档 chip（快问 2 万/标准 5 万/深度 12 万）点选即切换，随请求提交 tier，仪表跟随。
- **作用与目标**：把"成本 vs 完整度"权衡交还用户。

### 小节点 15.10 运行历史列表
- **做什么**：读 RunLog 倒序渲染（状态/问题/时间/token），点击进入详情回放，选中高亮。
- **作用与目标**：给用户"跑过的都在"的资产感——列表即入口，一点进回放（15.5），复盘闭环。
- **注意**：⚠P47 初始 `loadHistory()` 调用曾丢失导致左栏永不显示——函数定义了还要有人调用它。

### 小节点 15.11 运行状态条与交互细节
- **做什么**：运行中顶部显示状态条（⏳ 运行中 · 第 N/20 步 · 档位 · 计时器走动）；运行中禁用档位按钮；提问框支持 Ctrl/⌘+Enter 提交；断流时**先自动重连**（见 15.12），重连期间状态条显示"连接中断，自动重连中(n/5)"。
- **作用与目标**：运行中的"状态感知"——长任务最怕不知道卡没卡；断连必须有交代。
- **注意**：状态条随 done/error 隐藏；5 次重连全部失败才降级为"连接中断"卡片，且明确告知运行仍在后台继续、结果可从历史回放查看。

### 小节点 15.12 SSE 心跳与断线自动重连（⚠P54）
- **做什么**：三层防御对抗"长静默期连接被掐"（fetch_js 起无头浏览器渲染、模型长思考期间 SSE 一个字节都不发，空闲连接会被 WebView/代理掐掉）：
  1. **服务端心跳**：空闲超过 15 秒发 SSE 注释帧 `: ping`（浏览器 onmessage 忽略，但连接保持"活跃"，同时兼做探活）；
  2. **断点续传**：事件接口支持 `?since=N`——前端记录已收到的帧数，重连只取增量，不重放不漏发；
  3. **前端自动重连**：`onerror` 不再一次放弃——最多 5 次退避重连（0.8s×n），重连成功复位提示；5 连败才显示"连接中断"卡片。
- **作用与目标**：⚠P54 实测第 7 步 fetch_js 渲染时前端断流、用户以为运行停了，事后查存档发现 agent 一路跑到第 10 步正常结题——**断流不等于停跑，报告一直都在，只是没送到屏幕**。CLI 时代报告直接打印终端没有传输层，上 Web 后多了 SSE 这层，传输问题曾被误读为"阈值触发出不了报告"。
- **注意**：诊断口诀——先查 `logs/runs.jsonl` 有没有该运行的收尾记录：**有 = 传输层问题，没有 = 内核问题**；`run_meta` 在队列索引 0，`since` 计数必须把首页事件算进去，否则重连会漏帧。

### 小节点 15.13 多轮会话视图（同一会话的全部轮次完整保留）
- **做什么**：时间线从"每次运行整屏重置"改为"**按轮拼接**"：
  1. **实时追问**：同一 session 内的新提问**不清空**时间线——追加一张"第 N 轮"用户输入卡片（含时间戳 + run_id），其后事件节点归属该轮；渲染状态分两层：`beginTurnRender()`（只复位本轮的分组/看板/报告面板）与 `resetTimeline()`（整屏重置）；
  2. **整会话回放**：点击历史任一条 → 识别所属会话（优先 RunLog meta 的 session_id，其次存档头部 `run_meta` 事件）→ `GET /api/sessions/{sid}` 取全部轮次 → 逐轮渲染"用户卡片 + 该轮完整事件流"，横幅显示"会话回放 · 共 N 轮查询"；
  3. **刷新恢复**：`start()` 把 session_id 写 localStorage，页面加载自动还原上次会话的完整时间线；"🆕 新研究"清除；
  4. **无缝续接**：回放旧会话后直接输入新问题，作为该会话第 N+1 轮实况续跑。
- **怎么做**：后端在运行启动时向事件流头部推 `run_meta` 事件（run_id/session_id/question，随队列与存档落盘）；新增 `GET /api/sessions/{sid}` 返回会话全部轮次；前端 addUserTurn / loadSessionView。
- **作用与目标**：会话的"聊天记录式"完整性——用户看得到自己问过什么、agent 每轮做了什么；旧存档（无 run_meta 时代）自动降级为单运行回放，不报错。
- **注意**：⚠P52 运行注册只能有一个属主（webapp 入口 `session.add_run`，内核结尾不得重复——否则会话历史每 run 记两条，且引用未定义变量）；`turnCount` 在启动失败时要回退编号。

### 小节点 15.14 常驻看板卡片与报告面板增强（前端）
- **做什么**：
  1. **任务核销看板卡片**：`pinned_checklist` 常驻执行流顶部——plan 事件（sub_questions）初始化为全 pending，checklist 事件实时刷新（✅/⏳/⬜ + 结论摘要 + 进度条 + N/M 已核销百分比），与模型每轮看到的看板同源同拍；
  2. **报告面板**：final 事件驱动显示，头部元信息"约 X 字 · 耗时 Ys · 消耗 Z tok"；📋 复制 MD（clipboard API + 非安全上下文 execCommand 兜底）/ ⬇ 下载 .md（Blob + 文件名带时间戳）；⛶ 全屏沉浸阅读（zen 模式，Esc 退出）；
  3. **快捷场景 chips**：四条预设研究问题一键填入并联动切换档位。
- **作用与目标**：把 4.0 的动态看板从"模型视角"翻译成"用户视角"——用户不用展开事件流也能一眼看到调研进度；报告作为一等交付物可带走、可沉浸阅读。

---

## 大节点 16：多轮研究会话（common/session.py + webapp 接线）

- **做什么**：把多次运行串成一个**有记忆的研究会话**——同一个话题可以持续追问、逐步深入，而不是每次都从零开始。三档能力递进：
  1. **第一档 · 笔记续跑**：同 session 共用一份 `notes.md`——第二轮不再重复检索第一轮已沉淀的事实，直接在已有认知上加深；
  2. **第二档 · 上下文延续**：每轮结束保存上下文快照（`save_context`：摘要 / 关键发现 / 待办），下一轮启动时经 `_session_context()` 注入 system——模型"知道上次研究到哪了"；
  3. **第三档 · 多轮对话**：用户自由追问（"那 R1 呢？""可以购买吗？"），模型结合注入的历史上下文理解指代。
- **怎么做**：
  - `Session`（`sessions/{sid}/` 目录）：`notes.md`（共享笔记）+ `meta.json`（创建时间 / runs 列表 / topic）+ `context.json`（跨轮快照）；`add_run` 注册运行、`read_notes/append_notes` 笔记读写、`load_context/save_context` 快照；
  - `SessionStore`（`sessions/` 目录级）：`create / get / get_or_create`（get 兼容从磁盘恢复已存在会话）；`is_new` / `run_count` 属性；
  - **Web 接线**：`ResearchIn.session_id`（空 = 新建）→ `get_or_create` → 注入 agent 构造参数 → 响应返回 `session_id` 供前端记住；**运行注册只在入口做一次**（agent 半路崩溃也留痕）⚠P52；
  - **前端联动**：见 15.13（轮次拼接视图）。
- **与内核的边界**：内核只认注入的 `session` 对象（duck typing：`read_notes / append_notes / load_context / save_context`）——CLI 可以不传（单轮），Web 传 SessionStore 实例；会话协议不进内核，换宿主不用改内核。
- **作用（产品价值）**：研究是连续决策不是一锤子买卖。"天津房价怎么样"→"那红桥区呢"→"现在能买吗"三条独立提问会重复检索、互不知情；会话化后第二轮少跑一半检索、第三轮直接基于已有发现给判断。**成本与体验同时改善**。
- **注意**：⚠P52 会话历史每个 run 只能记一条（重复注册 = 统计口径错乱）；session 数据含个人研究内容，已加入 `.gitignore` 不进仓库；`context.json` 的 key_findings 从 note_write 参数提取（截 200 字 × 最近 5 条），非模型摘要。

---

## 附一：大节点依赖关系速查

```
1 配置接入 ──► 2 消息状态 ──► 4 执行内核（agent_core）──► 8 多智能体（内核的组织化）
                 ▲               │        │                   │
3 工具系统 ───────┘               │        ▼                   ▼
（循环的手）                 5 上下文工程   7 安全件          10 模式选型（对照实验）
                             6 记忆系统   （HITL/预算/硬收尾）
（循环的大脑燃料）                 │                │
                             13 MCP（工具生态）     ▼
                                        11 评测 ──► 12 可观测 ──► 15 工作台 GUI（产品门面）
                                                    14 辅助设施（调试工具）
16 多轮会话 ──► 4 内核（session 注入：笔记续跑/上下文延续/多轮对话）
     └─────────► 15 工作台（轮次拼接视图 / /api/sessions / localStorage 恢复）
```

## 附二：产品决策映射（每个大节点对应一个产品决策）

| 大节点 | 对应的产品决策 |
|---|---|
| 1 配置接入 | 供应商不锁定，模型可替换 |
| 2 消息状态 | 状态设计 = 产品记忆的设计 |
| 3 工具系统 | 给非人类用户做产品设计（工具=功能） |
| 4 执行内核 | 放权多少、刹车在哪；一个内核多种形态 |
| 5 上下文工程 | 成本曲线与长任务可行性 |
| 6 记忆系统 | 个性化留存与遗忘策略 |
| 7 安全件 | 出错时谁来兜底、代价多小 |
| 8 多智能体 | 能力上限 vs 复杂度成本 |
| 9 结构化输出 | 交付物契约化、可核验 |
| 10 模式与选型 | workflow 还是 agent（用数据说话） |
| 11 评测 | 先建度量再做选择 |
| 12 可观测 | 上线后的日常运营抓手 |
| 13 MCP | 自建生态 vs 接入生态 |
| 15 工作台 | 过程透明 + 用户控制权产品化 |
| 16 多轮会话 | 研究是连续决策，不是一锤子买卖 |
